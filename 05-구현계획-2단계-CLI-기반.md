# 모비노기 AI 도우미 — 구현 계획 2단계: CLI 기반

| 항목 | 내용 |
|---|---|
| 문서 상태 | 검토 대기 |
| 작성일 | 2026-09-21 |
| 대응 설계 | 02 §16의 2단계, 03 §2·§3·§4·§5.4·§6·§10 |
| 브랜치 | `feat/stage2-cli-base` |
| 다음 관문 | 계획 서브에이전트 리뷰 → 구현 → 구현 리뷰 → 테스트 리뷰 |

## 1. 목표

02·03이 승인한 규칙을 지키는 **명령 실행 기반**을 만든다. UI 없이 파이썬 패키지로 만들고,
기존 `mabinogi_chat.py`를 이 기반 위의 얇은 명령행 도구로 바꿔서 03 §10이 지적한
결함을 없앤다. 이 단계에서는 상태 감지·스냅샷·AI·GUI를 만들지 않는다.

## 2. 범위

포함:

1. `CliAdapter` — CLI 탐색(환경 변수 → 기본 경로 → `PATH`), 프로세스 실행, 제한 시간,
   stdout·stderr 수집, UTF-8 JSON 파싱, 종료 코드·실행 시간을 담은 원시 결과. argv를 기록하지 않는다.
2. 채팅 입력 판정 — 03 §6.1의 정규화·판정과 §6.2의 길이(사용자 입력 최대 48자). 화면 모드
   표시와 전송이 같은 함수를 쓴다(예외를 던지지 않는 판정 값).
3. `write_chat` 응답 판정표 — 03 §6.6. 전송 확정 / 미전송 확정 / 불명. **성공·거부
   `status` 목록의 초기값은 비어 있고**, 비어 있는 동안 모든 결과는 불명이다.
4. `ActionContext`·`Outcome`·저장 — 03 §3·§4. 단계 write-ahead 기록, 채팅 문구 미저장.
5. `origin` 강제 — 03 §5.4. `send_chat` 계열은 `origin` 없이 전송하지 않는다. `AI_PREFILL`은
   전송하지 않는다. 붙여넣기 유래는 확인 값(`confirmed=True`)이 있어야 `USER_AT`가 된다.
6. 최소 `CommandBroker` — 앱 내 CLI 호출을 직렬화하고, 요청 ID·세대 번호·명령 분류를
   부여한다. 동일 문구 재전송 확인(60초 또는 직전 `UNRESOLVED`, 메모리 해시)과 전송 중
   잠금을 포함한다.
7. 모의 CLI(`tests/fake_cli.py`) — 시나리오 파일로 (종료 코드, stdout, 지연, 종료 방식)을 지정한다.
8. `mabinogi_chat.py` 개편 — 위 기반을 호출하고, 결과를 사용자 문구(03 §6.4)로 출력한다.

제외(다음 단계): 게임 상태 감지·전이(3단계), 스냅샷·화해·대기 슬롯·초안(4단계), 선제 제안(5단계),
AI 대화(6단계), GUI(7단계), `QUERY`의 주기 조회와 스키마 캐시.

## 3. 파일 구조

```
mobinogi_helper/
  __init__.py
  chat_input.py      입력 정규화·판정·길이, ChatPlan(이모지·행동 규칙)
  cli_adapter.py     CLI 탐색·실행·RawResult
  response_judge.py  write_chat 응답 판정표(SendVerdict), 알려진 응답 설정
  action_context.py  Outcome, ActionContext, ActionStore(write-ahead, JSON)
  origin.py          Origin 열거값과 전송 허용 규칙
  broker.py          CommandBroker: 직렬화, ID, 채팅 실행 파이프라인
mabinogi_chat.py     명령행 도구(얇은 껍데기)
config/known_responses.json   성공·거부 status 목록(초기값 비어 있음)
tests/
  fake_cli.py
  test_chat_input.py, test_cli_adapter.py, test_response_judge.py,
  test_action_context.py, test_origin.py, test_broker.py, test_mabinogi_chat.py
```

## 4. 핵심 동작

### 4.1 채팅 실행 파이프라인(`CommandBroker.send_chat_action`)

1. 입력 판정 결과가 채팅이고 거부가 아닐 것, `origin`이 허용될 것(5.4)을 검사한다.
2. 동일 문구 재전송이면 확인 값이 없을 때 전송하지 않고 "확인 필요"를 돌려준다.
3. `ActionContext`를 만들고 채팅 단계를 `pending`으로 저장(write-ahead)한 뒤 CLI를 호출한다.
4. 응답을 `response_judge`로 판정하고 단계를 갱신한다(`sent`/`failed`/`unknown`).
5. 채팅이 전송 확정이고 행동이 있으면 같은 방식으로 행동 단계를 실행한다.
6. 단계 결과로 `Outcome`을 정한다(03 §6.4의 표): 완료 / 일부 완료(행동 실패 확정 또는 불명) /
   실패(미전송 확정) / 확인 불가(채팅 불명).
7. 채팅이 불명이면 행동을 실행하지 않는다.

### 4.2 응답 판정(03 §6.6)

- 종료 코드 0이고 `status`가 `sent_statuses`에 있으면 전송 확정.
- `verified`가 참이고 `status`가 `rejected_statuses`에 있으면 미전송 확정. 코드 5의
  `game_off`·`option_off` 행은 `[가정]`이므로 이 단계에서는 사용하지 않고 불명으로 둔다.
- 그 외 모두 불명: 시간 초과, 프로세스 종료·EOF, JSON 오류, 배열 응답, 알 수 없는 코드·`status`.
- `blocked`, `timeout`, `canceled`, `retryAfterSeconds`가 있는 응답은 성공이 아니다.
  `retryAfterSeconds`는 결과에 값으로 남기되 재시도하지 않는다.
- 채팅 프로세스 제한 시간은 15초 `[후보]`이며 초과 시 프로세스를 정리하고 불명으로 판정한다.

### 4.3 알려진 응답 설정과 실측

`config/known_responses.json`은 `{"verified": false, "sent_statuses": [], "rejected_statuses": []}`로
출고한다. 이 상태에서는 모든 채팅이 "확인 불가"로 끝나며 실제로 게임에 전달됐을 수는 있다.
목록을 채우려면 **사용자 승인 아래 채팅 1건을 실제 게임에 보내 응답 표본을 확보**해야 한다
(03 §9, 별도 작업). 이 계획의 구현·테스트는 실제 게임을 호출하지 않는다.

### 4.4 저장

`ActionStore`는 작업 컨텍스트를 JSON 파일에 저장한다(경로는 사용자 데이터 폴더, 환경 변수로 재정의).
저장 필드는 03 §3 표의 "예"인 항목뿐이며 채팅 문구·argv·stdout 원문은 저장하지 않는다.
시작 시 미완료 컨텍스트(`started_at`만 있는 항목)를 읽어 `CHAT`·`BEHAVIOUR`는 `UNRESOLVED`로
마감한다(02 C31). `ACTION`은 이 단계에서 만들지 않는다.

## 5. 완료 조건

1. 아래 시나리오의 자동 테스트가 모두 통과한다(모의 CLI 기반, 실제 CLI 호출 0건).
   - 03 T1~T10, T17~T20, T31~T36, T40~T45의 CLI·판정·`origin` 부분, 02 C2, C15~C17, C32, C33, C36의 채팅 경로
   - 오류 주입: 잘못된 JSON, JSON 배열, 빈 응답, 대용량 출력(1 MiB), 지연 초과, 프로세스 비정상 종료, 알 수 없는 `status`
2. `mabinogi_chat.py`가 03 §10의 결함 9개를 해결한다(§6 표).
3. 채팅 문구와 argv가 로그·저장 파일·오류 문구에 없음을 자동 검사한다(원문과 Base64 형태 모두).
4. 성공 목록이 빈 초기 상태에서 모든 채팅이 "확인 불가"이고 행동이 실행되지 않음을 테스트한다.
5. 서브에이전트 구현 리뷰와 테스트 리뷰에서 높은 중요도 0건.
6. 알려진 제한과 미검증 항목을 결과 문서(`reviews/05-구현-2단계-결과.md`)에 기록한다.

## 6. 03 §10 결함과 해결 방법

| 결함 | 해결 |
|---|---|
| 오류 없음을 성공으로 판정 | `response_judge` 판정표, 초기 성공 목록 비어 있음 |
| 0이 아닌 종료 코드를 연결 실패로 단정 | 종료 코드는 판정표 입력이며 단정하지 않고 불명 |
| 전송 제한 시간 없음, Ctrl+C 처리 | `CliAdapter` 제한 시간, 전송 중 인터럽트를 불명으로 마감하고 안내 |
| JSON 배열 응답 예외 | 검증기가 배열을 프로토콜 오류로 분류, 결과는 불명 |
| 채팅 성공 뒤 행동 실패 정보 소실 | 단계별 결과와 `PARTIAL` |
| 알 수 없는 `status`를 성공 처리 | 판정표 |
| 예외 기반 입력 판정 | `judge_input`이 값 객체를 반환 |
| 부분 문자열 행동 규칙 | 부정·문맥 예외 목록으로 보강(예: `안 고마`, `사과 팔`), 규칙 테스트 |
| 오류 출력에 stdout 전체 | 분류된 메시지만 출력 |

## 7. 위험과 대응

- **성공 응답 형식 미확인**: 초기값을 비우고 fail-closed로 둔다. 사용자가 실측 뒤 채운다.
- **실제 CLI 오호출 위험**: 테스트는 `MABINOGI_MOBILE_CLI`를 모의 CLI로 고정하고, 기본 경로
  CLI가 테스트에서 실행되면 실패하도록 가드한다.
- **동시 호출**: 브로커의 잠금으로 직렬화하고, 전송 중 재진입은 거부한다.
- **파이썬 3.14와 Windows 인코딩**: UTF-8 명시, 경로 공백·한글 테스트를 포함한다.

## 8. 진행 순서

1. 이 계획서 리뷰(아키텍처·안전) → 승인
2. 구현(테스트 먼저 또는 병행)
3. 구현 리뷰 → 수정
4. 테스트 리뷰 → 수정
5. 결과 문서와 승인 기록 → 로드맵 갱신

## 9. 리뷰 기록

| 날짜 | 상태 | 내용 |
|---|---|---|
| 2026-09-21 | 검토 대기 | 최초 계획서 작성 |
