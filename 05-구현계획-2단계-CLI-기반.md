# 모비노기 AI 도우미 — 구현 계획 2단계: CLI 기반

| 항목 | 내용 |
|---|---|
| 문서 상태 | 재검토 대기 |
| 작성일 | 2026-09-21 |
| 수정일 | 2026-09-21 |
| 대응 설계 | 02 §6·§16·§16.1, 03 §2·§3·§4·§5·§6·§8·§10 |
| 브랜치 | `feat/stage2-cli-base` |
| 관련 리뷰 | `reviews/05-구현계획-리뷰-1.md`, `reviews/05-구현계획-리뷰-2.md` |
| 다음 관문 | 계획 재리뷰 → 구현 → 구현 리뷰 → 테스트 리뷰 |

## 1. 목표

02·03이 승인한 규칙을 지키는 **명령 실행 기반**을 파이썬 패키지로 만든다. 기존
`mabinogi_chat.py`는 이 기반 위의 얇은 명령행 도구로 바꿔서 03 §10의 결함을 없앤다.
이 단계에서는 상태 감지·스냅샷·AI·GUI를 만들지 않는다.

## 2. 범위

포함:

1. **`CliAdapter`** — CLI 탐색, 프로세스 실행, 제한 시간(없음도 허용), 출력 상한, 프로세스 트리
   정리, UTF-8 JSON 파싱, 원시 결과. 어댑터는 **사실만** 담는다(`RawResult.failure`: 시간 초과, 실행 실패, 출력 상한 초과, 비정상 종료 등). 의미 판정은 하지 않는다(02 §6.1). argv를 기록하지 않으며 예외에서 argv를 제거한다.
2. **명령별 응답 검증기**(`response_validator.py`) — `status`, `capabilities`, 일반 조회, 채팅
   응답의 계약 검증. 배열·필수 필드 누락 같은 **계약 위반만** 분류한다(`PROTOCOL_ERROR` 후보 사유). `loading=true`는 정상 응답(대기 재시도)이며 5분 초과 판단은 3단계 감독기 소관이다(02 G11).
3. **명령 분류 표**(`command_catalog.py`) — 03 §2의 `QUERY`/`CHAT`/`BEHAVIOUR`/`ACTION` 내장 표,
   허용 행동 목록, `requiresConfirm` 정규화(문자열·불리언·누락·미지 값 → 확인 필요).
4. **채팅 입력 판정**(`chat_input.py`) — 03 §6.1의 정규화·판정과 §6.2의 48자. 예외를 던지지
   않는 값 객체 `InputJudgment`. 이모지·행동 정책은 `chat_plan.py`로 분리한다.
5. **응답 판정표**(`response_judge.py`) — 03 §6.6. 판정 값 `SendVerdict`는 종류(전송 확정 / 미전송
   확정 / 불명)와 **사유 열거값**(시간 초과, 프로세스 종료, JSON 오류, 배열 응답, 알 수 없는
   코드·status, 출력 상한 초과, 사용자 중단, 알려진 응답 미설정, 거부 응답 등)을 함께 담는다.
   3단계 감독기가 사유를 상태 전이(02 G13·G16·G17·G22·G23)로 옮길 수 있게 한다. 사유 열거는 공용 `reasons.py`가 소유한다. 어댑터는 사실(`failure`)만, 검증기는 계약 위반만, 판정기·조회 판정이 이를 사유로 옮긴다. 채팅 응답의 JSON 형태 검사는 검증기가 하고 판정기는 검증 결과만 받는다(이중 판정 없음).
6. **`ActionContext`·`Outcome`·저장**(`action_context.py`) — 03 §3·§4. 원자적 쓰기, 스키마 버전,
   단계 write-ahead. **저장 실패 시 전송하지 않는다.** 채팅 문구·argv·stdout 원문은 저장하지 않는다.
7. **출처 규칙**(`origin.py`) — 03 §5.4. 입력 유래 `Provenance`(TYPED, PASTED, PREFILLED)와
   `Origin`(USER_AT, AI_APPROVED, SYSTEM, AI_PREFILL)을 다룬다.
8. **`CommandBroker`(최소)** — 명령 직렬화, 요청 ID, 세대 번호(`bump_generation()`), `CommandState`(`IDLE`, `RUNNING_QUERY`, `RUNNING_SEND`, 일시 `FAILED`) 전이와 관찰자 콜백(3단계 감독기가 전이를 받는 통로), 단발 `QUERY` 실행(`QueryResult(kind, reason, generation, request_id)`, 제한 시간 5초 `[후보]`), 우선순위(`run(priority, wait_ms)`: 사용자 명령은 짧게 기다린 뒤에도 못 잡으면 `BUSY`). 채팅 의미는 몰라야 한다.
8a. **`ChatService`**(`chat_service.py`) — 채팅 실행 파이프라인, 행동만 다시 보내기, 동일 문구·미확인 `UNRESOLVED` 재전송 확인. 브로커 위에서 동작한다.
9. **모의 CLI**(`tests/fake_cli.py`) — 시나리오 파일로 (종료 코드, stdout, stderr, 지연, 종료 방식,
   에코 여부)를 지정하고 호출 로그(테스트 전용, 검사 대상 제외)를 남긴다.
10. **`mabinogi_chat.py` 개편** — 기반을 호출하고 03 §6.4 문구와 종료 코드로 결과를 알린다.
11. **표본 확보 도구**(`tools/capture_sample.py`) — 사용자가 직접 실행하는 도구. 승인 문답 뒤 채팅
    1건을 보내고 종료 코드·최상위 키 이름·`status` 값만 화이트리스트로 출력한다. 자동 테스트에서
    실행하지 않는다.
12. **문서 정정** — README §7과 FAQ의 "전송 완료" 설명, 00-로드맵 문서 지도에 05 추가.

제외(다음 단계): 게임 상태 감지·전이(3단계), 스냅샷·화해·대기 슬롯·초안·`ACTION` 실행(4단계), 선제
제안(5단계), AI 대화(6단계), GUI(7단계), 주기 조회·스키마 캐시.

## 3. 파일 구조

```
mobinogi_helper/
  __init__.py
  reasons.py           공용 사유 열거
  cli_adapter.py       CLI 탐색·실행·RawResult(사실만), AdapterError
  response_validator.py  명령별 응답 계약 검증
  command_catalog.py   명령 분류, 허용 행동, requiresConfirm 정규화
  chat_input.py        입력 정규화·판정·길이(InputJudgment)
  chat_plan.py         이모지·행동 규칙(부정·문맥 예외 포함)
  response_judge.py    write_chat 판정표, 알려진 응답 설정 로드
  action_context.py    Outcome, ActionContext, ActionStore
  origin.py            Provenance, Origin, 전송 허용 규칙
  broker.py            CommandBroker, CommandState, QueryResult
  chat_service.py      채팅 파이프라인, 행동 재전송, 재전송 확인
mabinogi_chat.py       명령행 도구
tools/capture_sample.py
config/known_responses.json
tests/  (모의 CLI, 공통 기반, 모듈별 테스트, 추적표 테스트)
```

## 4. 핵심 결정

### 4.1 CLI 탐색과 실제 CLI 호출 방지

- **탐색 규칙**: 환경 변수 `MABINOGI_MOBILE_CLI`가 **설정돼 있으면 그 경로만** 쓴다. 파일이 없으면
  오류로 끝내고 다음 후보로 넘어가지 않는다. 설정돼 있지 않을 때만 기본 경로, 그다음 `PATH`
  순으로 찾는다. `PATH`는 현재 폴더를 먼저 잡지 않도록 절대 경로 항목만 쓴다.
- **어댑터 시그니처**: `CliAdapter(executable, prefix_args=())`. 모의 CLI는 `executable=sys.executable`,
  `prefix_args=[fake_cli.py]`로 실행해 `.py` 직접 실행 문제를 피하고 운영 코드와 같은 경로를 검증한다.
  `run(command, args, timeout=None, cancel_event=None, max_output=1 MiB)`이며 `timeout=None`은
  장시간 작업용(4단계)이다.
- **테스트 가드(완료 조건)**: 가드는 `tests/__init__.py`에서 패키지 로드 시 설치해 **모든 테스트 모듈**(기존 `tests/test_mabinogi_chat.py`와 메타 테스트 포함)에 적용한다. 공통 기반 `setUp`이 아니라 패키지 수준이다.
  (1) `subprocess.Popen`, `os.system`, `os.spawn*`, `os.startfile`, `asyncio` 서브프로세스 생성 함수를 감싼다. **허용 규칙**은 실행 파일 = `sys.executable`이고 첫 인자 = 모의 CLI 스크립트 경로일 때, 그리고 정리용 `taskkill`이 테스트가 만든 프로세스 PID를 대상으로 할 때뿐이다. 그 밖의 호출은 `GuardViolation`(`BaseException` 계열)을 던진다 — 넓은 `except Exception`에 삼켜지지 않게 한다. 또한 위반 횟수를 세어 모든 테스트의 `tearDown`이 0인지 검사한다.
  (2) `MABINOGI_MOBILE_CLI`를 모의 CLI로 고정한다. (3) 저장 경로 환경 변수를 임시 폴더로 고정하고 모의 CLI 호출 로그는 저장 폴더와 **별도 임시 폴더**에 둔다(원문 검사 대상에서 분리).
  **측정 문구**: "실제 CLI 호출 0건"은 가드 위반 카운터가 0인 것으로 측정한다(모의 CLI 로그는 실제 CLI 호출 여부를 알 수 없다). 실행 파일 없음은 어댑터의 `is_file` 사전 검사로, 권한 거부는 가드가 허용한 범위 안에서 `PermissionError`를 주입해 시험한다. 탐색 순서 테스트는 실행 없이 `is_file`만 대체한다.
- **예외 위생**: 어댑터는 `subprocess`의 예외를 잡되 **except 블록 밖에서** argv를 뺀 `AdapterError`를 새로 던진다(`from None`만으로는 `__context__`가 남는다). `json.JSONDecodeError`도 원문(`doc`)을 보관·전파하지 않고 위치와 종류만 남긴다.
- **프로세스 트리 정리**: 시간 초과·중단·출력 상한 초과 때 프로세스 트리를 정리한다(Windows는 새 프로세스 그룹과
  `taskkill /T /F`). 정리 뒤 파이프 읽기가 멈추지 않도록 읽기 스레드 join에 제한 시간을 둔다(손자 프로세스가 분리돼 파이프를 붙잡는 경우 포함).
- **출력 상한**: 1 MiB `[후보]`를 넘으면 읽기를 멈추고 `OUTPUT_TOO_LARGE`(불명)로 마감한다.

### 4.2 응답 판정(03 §6.6)

- 판정 입력은 최상위 `status`(문자열일 때만), 종료 코드, 본문의 `error`·`message`·`retryAfterSeconds`다.
  `status`가 문자열이 아니면(리스트·딕셔너리·없음) 불명이다.
- 다음 중 하나라도 있으면 성공이 아니다: `body.error` 또는 최상위 `error`, `retryAfterSeconds`,
  `status`가 `blocked`·`timeout`·`canceled`.
- **전송 확정**: 종료 코드 0, `config_verified=true`, `status`가 `sent_statuses`(정확 일치, 대소문자·공백 변형
  없음)에 있을 것. **미전송 확정**: `config_verified=true`이고 `status`가 `rejected_statuses`에 있을 것.
  코드 5의 `game_off`·`option_off` 행은 `[가정]`이므로 이 단계에서 쓰지 않는다.
- 그 외 모두 불명. 사유 열거값을 반드시 붙인다(빈 목록이면 `NO_KNOWN_RESPONSES`).
- 채팅 프로세스 제한 시간은 15초 `[후보]`이며 테스트에서는 주입한 짧은 값을 쓴다.

### 4.3 알려진 응답 설정

`config/known_responses.json`은 `{"schema": 1, "config_verified": false, "sent_statuses": [], "rejected_statuses": []}`로
출고한다. `config_verified`는 **두 목록 모두의 사용 여부**를 정한다(거짓이면 목록이 있어도 무시. 응답 본문의 필드와 무관). 파일이 없거나
손상됐거나 형식이 틀리면(목록이 문자열, 값이 문자열이 아님 등) 빈 설정으로 처리하고 사용자에게 알린다.
출고 상태에서는 모든 채팅이 "확인 불가"로 끝나고 행동은 실행되지 않는다. 목록은 §4.9의 표본 확보 도구로
사용자가 승인해 얻은 값으로 채운다. 이 계획의 구현·테스트는 실제 게임을 호출하지 않는다.

### 4.4 출처와 확인 (03 §5.4, T31·T45)

- `Provenance`: `TYPED`(키 입력으로 전부 새로 작성), `PASTED`(붙여넣기·음성·끌어다 놓기·복원), `PREFILLED`
  (AI가 채운 문구 또는 그것을 수정한 문구; 전부 새로 작성한 경우만 `TYPED`).
- 전송 허용 규칙: `USER_AT`는 `TYPED`이거나 `PASTED`+`paste_confirmed`. `PREFILLED`는 `USER_AT` 불가.
  `AI_PREFILL`·`SYSTEM`은 항상 거부. `AI_APPROVED`는 `action_id`에 묶인 승인 객체(`Approval`)가
  있어야 하며 승인 없이는 거부한다(2단계에는 발급자가 없으므로 거부 경로와 유효성 테스트만 둔다).
- 확인 값은 분리한다: `paste_confirmed`(붙여넣기 확인), `duplicate_confirmed`(동일 문구 재전송 확인).
- **명령행 도구의 출처 판정**: 표준 입력이 **터미널일 때만** 대화 모드의 입력 줄을 `TYPED`로 인정한다(파이프 입력이면 인정하지 않고 인자 모드와 같이 취급). 인자 모드(`argv`)는 스크립트나 다른 프로그램이 호출할 수 있어 `TYPED`가 아니다. 터미널이면 미리보기를 보인 뒤 아래 문답을 거쳐 `PASTED`+`paste_confirmed`가 되고, 터미널이 아니면 전송을 거부하고 종료 코드 6으로 끝낸다. 한계: 터미널에서 실행한 스크립트가 tty를 상속하면 이 판정이 막지 못한다(문서화).
- **확인 문답(H1)**: 문답은 두 가지이며 서로 대신할 수 없다. **질문 1** `보낼까요? [y/N]` — 정확히 `y`/`Y` 한 글자만 긍정이고 그 밖은 취소다. 질문을 하기 전에 버퍼에 남은 입력을 비워 붙여넣은 여러 줄이 답으로 소비되지 않게 한다(여러 줄은 애초에 거부, T6). **질문 2** `이미 전송됐을 수 있어요. 게임 채팅창에서 확인했나요? 그래도 다시 보낼까요? [y/N]` — 동일 문구를 60초 안에 다시 보내는 경우, 또는 확인하지 않은(ack 안 된) `UNRESOLVED` 기록이 있는 경우에만 한다. `--yes` 같은 우회 플래그는 없다.
- **`UNRESOLVED` 확인 해제**: 저장된 각 `UNRESOLVED` 컨텍스트에는 `acknowledged` 플래그가 있다. 질문 2에 사용자가 `y`를 답하면 확인하지 않은 `UNRESOLVED` 기록을 **모두** ack 처리하고 그 뒤 새 전송에는 질문 2를 묻지 않는다(새 `UNRESOLVED`가 생기기 전까지). 출고 상태에서는 모든 전송이 `UNRESOLVED`가 되어 다음 전송마다 질문 2가 나오므로 이 도구는 표본 확보 전에는 사용을 권하지 않고 안내 문구에 밝힌다. 60초 동일 문구 확인은 메모리 해시이고 재시작 뒤에는 저장된 미확인 `UNRESOLVED`가 대신한다(03 §5.4의 재시작 뒤 안내를 강화한 것이며 03 §5.4에 반영한다).
- **미리보기(T33)**: 명령행에는 실시간 미리보기가 없으므로, 대화 모드도 줄을 입력하면 최종 전송 내용
  (정규화·이모지·자동 행동)을 보이고 `보낼까요? [y/N]`으로 한 번 더 확정을 받는다. 자동 행동은 환경
  설정으로 끌 수 있다. 미리보기 없이 행동이 나가는 경로는 없다.

### 4.5 채팅 실행 파이프라인 (`ChatService.send_chat_action`)

시그니처: `send_chat_action(judgment, *, origin, provenance, paste_confirmed=False, duplicate_confirmed=False, approval=None)`. `origin`이 `None`이면 전송하지 않는다(T32).

1. 입력 판정이 채팅·비거부이고 출처 규칙을 통과하는지 검사한다. 이미 전송 중이면 `BUSY`를 돌려준다.
2. 동일 문구를 60초 안에 다시 보내거나 확인하지 않은 `UNRESOLVED`가 있으면 `duplicate_confirmed`가 없을 때 전송하지
   않고 "확인 필요"를 돌려준다. 비교 값은 최종 전송 문자열의 **메모리 해시**이며 CLI 호출 전에 등록한다.
   프로세스가 다시 시작된 뒤에는 저장 파일에 남은 미완료·`UNRESOLVED` 컨텍스트를 발견하면 문구 비교 없이
   "직전 전송이 확인 불가였어요. 게임 채팅창을 먼저 확인해주세요"를 알리고 새 전송에 `duplicate_confirmed`를
   요구한다.
3. `ActionContext`를 만들고 단계를 `pending`으로 **저장한 뒤** CLI를 호출한다. 저장이 실패하면 전송하지 않는다.
4. 응답을 판정하고 단계를 갱신한다(`sent`/`failed`/`unknown`). 사용자 중단(Ctrl+C)은 불명으로 마감한다.
5. 채팅이 전송 확정이고 행동이 있으면 같은 방식으로 행동 단계를 실행한다. 채팅이 불명·미전송이면 행동을
   실행하지 않는다.
6. `Outcome`(03 §4): 완료 / 일부 완료(행동 실패 확정 또는 불명) / 실패(미전송 확정) / 확인 불가(채팅 불명).
   `행동 불명`이면 `행동만 다시 보내기`를 허용하지 않는 플래그를 결과에 담는다.
7. **행동만 다시 보내기**(`resend_behaviour`): `PARTIAL`이고 행동 실패가 확정일 때만 `USER_AT`(사용자 버튼)로
   허용하며 채팅은 다시 보내지 않는다. 행동은 허용 행동 목록에 있어야 한다(T34~T36).
8. **세대 번호**: 브로커는 요청마다 세대를 부여하고 `QUERY` 응답이 오래된 세대면 폐기한다. 채팅 완료
   응답은 세대와 무관하게 수용한다(02 §5).

### 4.6 저장

`ActionStore`는 JSON 파일(사용자 데이터 폴더, 환경 변수로 재정의)에 임시 파일 → `os.replace`로 원자적으로
쓰고 `schema_version`을 넣는다. 파일이 손상되면 백업 이름으로 옮기고 새로 시작한다. 저장 필드는 03 §3의
"예"인 항목뿐이다. 행동 이름(`/하트`)은 문구에서 유도되므로 `BEHAVIOUR` 컨텍스트에는 저장하지 않고
단계 상태만 남긴다. `label`은 명령 종류로 만들며 채팅 문구를 포함하지 않는다. 컨텍스트에는 소유 프로세스(`owner_pid`, `owner_start`)를 기록한다. 시작 시 `pending` 단계가 있는 컨텍스트 중 **소유 프로세스가 죽은 것만** `UNRESOLVED`로 마감한다(02 C31, 03 T22). 살아 있는 소유자의 컨텍스트는 건드리지 않는다.

**알려진 제한**: 브로커의 직렬화는 한 프로세스 안에서만 유효하다. 대화형 도구와 인자 모드를 동시에 실행하면
직렬화·메모리 재전송 확인이 공유되지 않는다. 단일 실행 잠금(02 §6.3)은 7단계에서 구현한다.

### 4.7 출력과 안내

- 출력은 예외 없이 쓰도록 `safe_print`(인코딩 오류 대체)를 쓰고 표준 출력 인코딩이 cp949여도 전송 결과
  안내가 실패로 오해되지 않게 한다(테스트 포함).
- **종료 코드**: 0 전송 확정, 2 입력 거부, 3 확인 불가, 4 실패(미전송 확정), 5 일부 완료, 6 전송 확인 필요
  (재전송 확인 미충족), 1 예기치 않은 오류.
- **확인 불가 문구**: 사유가 `NO_KNOWN_RESPONSES`이면 "전송됐을 수 있지만 결과를 확인할 수 없어요. 이 도구는 아직 게임의
  성공·거부 응답 형식을 배우지 못했어요(표본 미확정). 게임 채팅창에서 확인해주세요. 다시 보내지 않았어요."로, 그
  밖의 사유이면 "전송됐는지 확인할 수 없어요. 다시 보내지 않았어요. 게임 채팅창에서 확인해주세요."로
  안내한다. 두 경우 모두 자동 재전송하지 않는다. 오류 출력에 CLI stdout·stderr 원문을 넣지 않는다.

### 4.8 채팅 행동 규칙 보강 (03 §10)

부분 문자열 규칙에 부정·문맥 예외를 둔다(`안 고마`, `사과 팔`, `사과나무` 등). 예외 목록이 끝없이 늘어나지
않도록 행동은 **뚜렷한 표현일 때만** 붙이는 방향으로 좁힌다. 규칙과 예외 테스트를 둔다.

### 4.9 표본 확보 도구

`tools/capture_sample.py`는 사용자가 직접 실행하며 **어댑터를 직접 사용하는 명시적 예외**다(브로커·저장을 거치지 않고 저장하지 않는다). 시작하면 무엇이 게임 채팅으로 나가는지 알리고 승인 문답을
거친 뒤 고정 문구 1건을 보낸다. 출력은 종료 코드, 응답의 최상위 키 이름, `status` 값(문자열이면)만 화이트리스트로
보여주며 원문은 저장하지 않는다. 성공 표본은 채팅 1건을 보낸 뒤 사용자가 **게임 채팅창에 실제로 떴는지** 답하게 하고, 떴다고 답한 경우에만 그 `status` 값을 성공 후보로 안내한다. 거부 표본은 게임에 전달될 수 없는 형식의 본문(`--reject-sample`, 잘못된 Base64)을 보내 얻는다. 사용자가 그 값을 확인하고 설정 파일에 직접 반영한다. 자동 테스트에서 실행하지
않는다.

## 5. 완료 조건

1. 아래 시나리오 테스트가 모의 CLI 기반으로 모두 통과하고 실제 CLI 호출은 0건이다(§4.1).
   - 02: C1, C2, C10~C17, C32, C33, C36, C31(채팅 경로), 세대 번호 규칙(G34의 QUERY 폐기)
   - 03: T1~T10, T17~T20, T22, T31~T36, T40~T45, T18(요청 확인 정규화)
   - 오류 주입: 잘못된 JSON, JSON 배열, 빈 응답, 대용량(상한 초과·상한 이하), 지연 초과, 비정상 종료,
     알 수 없는 `status`, 종료 코드 2·3·4·5(본문 있음·없음), stderr만 출력, 잘못된 UTF-8 바이트, 실행 파일
     없음·권한 거부·공백·한글 경로, stdout 비고 종료 코드 0, 응답 뒤 지연 종료, 사용자 중단(Ctrl+C),
     손자 프로세스가 파이프를 붙잡는 경우
   - 설정: 파일 없음·손상·형식 오류, `config_verified=false`에 목록 있음, 목록이 문자열, `blocked`·`retryAfterSeconds`
     응답 + 성공 목록 일치는 불명
2. **T번호 추적표**: 각 테스트에 ID(`T5`, `C13` 등)를 태그하고, 범위 ID(위 1항)가 모두 존재하며 **각 ID의 테스트에 단언(`assert*`)이 하나 이상 있음**을 검사하는 메타 테스트를 둔다. 이 단계에서 검증하는 범위는 사유·결과 값·`Outcome`까지이며, 게임 축 전이(G)와 화면(T2 화면 모드, T36 버튼 표시)은 3단계 이후·7단계 소관이므로 표시만 한다.
3. 채팅 문구·argv·stdout 원문이 **로그 레코드, 저장 폴더의 모든 파일, 표준 출력·오류, 예외의 `str`·`repr`·트레이스백**
   어디에도 없음을 자동 검사한다. 고유 카나리 문구를 쓰고 모의 CLI가 argv와 stdout으로 카나리를 에코하는 시나리오를
   포함하며, 변형 검사는 Base64(표준·URL-safe·패딩 유무), JSON `\uXXXX` 이스케이프, UTF-16·cp949 바이트, 퍼센트 인코딩과 6자 이상의 부분 조각까지 포함한다. 알려진 한계: 명령줄의 Base64 본문은 같은 PC의 프로세스 목록에서 보일 수 있다(03 §8). `label`에도 문구가 없음을 검사한다.
4. 출고 설정(성공 목록 비어 있음)에서 모든 채팅이 "확인 불가"이고 행동이 실행되지 않음을 **고정 픽스처**로 테스트한다(저장소의 `config/known_responses.json`을 읽지 않으므로 사용자가 표본을 채워도 테스트는 깨지지 않는다).
   성공·거부 목록이 필요한 T1, T7, T35, T44는 **테스트 전용 설정 주입**을 쓴다.
5. 중복 채팅 0건·자동 상태 변경 0건: 채팅 경로에 한정해 난수 시드를 고정한 퍼징(200회 `[후보]`, 03 §9.1의 1,000회 요구를 2단계에는 200회로 조정하며 이를 03 §9.1과 02 §16.1에 반영한다. 무작위 JSON·빈 응답·
   대용량·지연·종료)을 돌리고 모의 CLI 호출 로그로 측정한다. 전체 1,000회 퍼징은 3단계 이후에 확장한다.
6. `mabinogi_chat.py`가 03 §10의 결함 9개를 해결한다(§6). 기존 `parse_chat_input`·`build_chat_plan` 호출 호환을
   위한 얇은 래퍼를 두고 기존 테스트를 갱신한다.
7. README §7·FAQ의 "전송 완료" 설명을 정정하고 00-로드맵에 05를 추가한다.
8. 서브에이전트 구현 리뷰와 테스트 리뷰에서 높은 중요도 0건.
9. 알려진 제한과 미검증 항목을 `reviews/05-구현-2단계-결과.md`에 기록한다.

## 6. 03 §10 결함과 해결 방법

| 결함 | 해결 |
|---|---|
| 오류 없음을 성공으로 판정 | 판정표, 출고 성공 목록 비어 있음 |
| 0이 아닌 종료 코드를 연결 실패로 단정 | 종료 코드는 판정 입력, 단정하지 않고 사유로 분류 |
| 제한 시간 없음, Ctrl+C 미처리 | 어댑터 제한 시간·트리 정리, 중단은 불명 마감 |
| JSON 배열 응답 예외 | 검증기가 배열을 계약 위반으로 분류, 결과 불명 |
| 채팅 성공 뒤 행동 실패 정보 소실 | 단계별 결과와 `PARTIAL` |
| 알 수 없는 `status`를 성공 처리 | 판정표 |
| 예외 기반 입력 판정, 정규화 없음 | `InputJudgment` 값 객체, 개행 검사 → NFKC·비가시문자(ZWJ·변형 선택자 보존) → 접두 검사 |
| 부분 문자열 행동 규칙 | 부정·문맥 예외, 행동을 뚜렷한 표현으로 제한, README 표현 조정 |
| 오류 출력에 stdout 전체 | 분류된 메시지만 출력, 예외에서 argv 제거 |

## 7. 위험과 대응

- **성공 응답 형식 미확인**: fail-closed(출고 목록 비어 있음)와 표본 확보 도구.
- **출고 직후 사용자 혼란**: 확인 불가 문구에 사유를 밝히고 README를 정정한다(§4.7). 사용자가 표본 확보를
  하기 전까지 도구는 모든 전송을 "확인 불가"로 안내한다. 표본 확보는 이 단계 구현 뒤 사용자 승인 아래 수행한다.
- **실제 CLI 오호출**: §4.1의 가드와 탐색 규칙.
- **동시 호출**: 재진입은 `BUSY`, 프로세스 간 경합은 제한으로 명시한다(§4.6).
- **파이썬 3.14·Windows**: UTF-8 자식 프로세스, cp949 표준 출력 테스트, 공백·한글 경로, 인터프리터 접두 실행.

## 8. 진행 순서

1. 계획 재리뷰(아키텍처·안전) → 승인
2. 구현(모듈별로 테스트 먼저 또는 병행)
3. 구현 리뷰 → 수정
4. 테스트 리뷰 → 수정
5. 결과 문서·승인 기록·로드맵 갱신
6. 표본 확보(사용자 승인, 별도)

## 9. 리뷰 기록

| 날짜 | 상태 | 내용 |
|---|---|---|
| 2026-09-21 | 검토 대기 | 최초 계획서 작성 |
| 2026-09-21 | 변경 필요 | 서브에이전트 2인 리뷰, 승인 보류 (`reviews/05-구현계획-리뷰-1.md`) |
| 2026-09-21 | 재검토 대기 | 검증기·명령 분류·출처 유래·실 CLI 가드·안내 문구·표본 도구·추적표 반영 |
