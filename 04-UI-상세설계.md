# 마비노기모바일 AI도우미 — UI 상세설계

| 항목 | 내용 |
|---|---|
| 문서 상태 | 변경 필요 |
| 작성일 | 2026-09-21 |
| 대상 | AI 사용이 익숙하지 않은 마비노기 모바일 PC 이용자 |
| 제품 형태 | Windows용 대화형 도우미 앱 |
| 선행 문서 | `02-연결-상태-설계.md` |
| 다음 관문 | 인터랙티브 목업과 독립 UI 리뷰 |

## 1. 제품 경험 목표

앱을 실행하면 사용자가 빈 화면에서 무엇을 물어볼지 고민하지 않도록 현재 게임
상태를 바탕으로 먼저 한 가지 도움을 제안한다. 사용자는 명령어를 외우지 않고
평소 말하듯 요청하며, 게임 채팅으로 보낼 때만 문장 앞에 `@@`를 붙인다.

핵심 원칙:

1. 첫 화면만 보고 게임 연결, AI 대화 가능 여부, 현재 작업 상태를 구분할 수 있다.
2. 기술 용어보다 “게임을 켜주세요”, “설정에서 연결 기능을 켜주세요”처럼 다음
   행동을 말한다.
3. 제안은 한 번에 하나만 강조하고 자동 실행하지 않는다.
4. 새 제안과 상태 갱신은 입력창 초점을 빼앗지 않는다.
5. AI가 없어도 연결 안내, 준비된 도움말, `@@` 채팅 같은 로컬 기능을 유지한다.

## 2. 입력 규칙

| 입력 예 | 처리 | 화면 표시 |
|---|---|---|
| `오늘 뭐 하지?` | AI에게 질문 | 도우미 답변과 실행 가능한 선택지 |
| `가방 상태 알려줘` | AI가 게임 조회를 계획 | 조회 중 표시 후 쉬운 설명 |
| `@@안녕하세요` | 게임 채팅 직접 요청 | `안녕하세요 😊` 전송 결과 |
| `@@ 고마워요` | 위와 동일 | `고마워요 😍` 뒤에 명확한 의도의 `/하트` |
| `@@`만 입력 | 전송하지 않음 | “보낼 말을 입력해주세요” |

`@@문구`와 `@@ 문구`는 동일하다. 모든 채팅에는 어울리는 이모지 하나를 붙이고,
감사·사과·축하처럼 의도가 분명할 때만 행동을 추가한다. 사용자가 직접 입력한
`@@`는 명시적 전송 요청이므로 별도 확인창 없이 보낸다. 장시간 작업 중에는 즉시
보내지 않고 대기 이유와 수정·취소 기능을 제공한다.

## 3. 앱 구조

### 3.1 상단 영역

- 제품명: `마비노기모바일 AI도우미`
- 독립 상태 3개: `게임`, `AI 대화`, `현재 작업`
- 상태는 아이콘, 짧은 문구, 색상을 함께 사용한다.
- 상태를 선택하면 한 줄 설명과 가능한 해결 행동을 펼친다.
- 우측에는 설정 하나만 두고 불필요한 메뉴를 만들지 않는다.

### 3.2 대화 영역

- 사용자와 도우미의 대화를 시간순으로 표시한다.
- 도우미 메시지는 설명 뒤에 최대 세 개의 구체적인 선택지만 제시한다.
- 게임 상태에서 나온 내용에는 관찰 시각과 `최신`, `오래됨`을 표시한다.
- 연결 오류는 대화 메시지를 계속 쌓지 않고 상단 상태 카드 하나를 갱신한다.
- 실행 결과는 `완료`, `일부 완료`, `결과 확인 중`, `실패`로 분리한다.

### 3.3 오늘의 제안

- 현재 가장 중요한 제안 하나만 대화 상단에 고정한다.
- 기본 행동은 `자세히 보기`이며 상태 변경은 사용자가 다시 선택해야 한다.
- 보조 행동: `1시간 조용히`, `오늘은 숨기기`, `이 제안 끄기`.
- 긴급 상태는 숨김보다 복구 행동을 우선하지만 자동 실행하지 않는다.

### 3.4 입력 영역

- 한 줄 입력창과 `보내기` 버튼을 둔다.
- 입력이 `@@`로 시작하면 테두리·아이콘·안내 문구가 `게임 채팅` 모드로 바뀐다.
- 그 외에는 `AI에게 부탁하기` 모드다.
- 입력창 아래에 `@@를 붙이면 게임 채팅으로 보내요`라는 짧은 도움말을 둔다.
- 전송 전 글자 제한을 실시간 표시하고 초과 시 이유를 텍스트로 알린다.

## 4. 핵심 화면

### 4.1 첫 실행

한 화면에서 다음 단계를 순서대로 검사한다.

1. 도우미 준비
2. 게임 찾기
3. 게임 실행 확인
4. 게임 설정의 연결 기능 확인
5. 캐릭터 접속 확인

완료된 단계는 접고, 막힌 단계 하나만 크게 보여준다. 가능한 행동은 `다시 확인`,
`설정 위치 보기`, `경로 직접 지정`, `진단 정보 복사`다. 기술 진단은 기본적으로
접어두고 개인정보와 채팅 문구는 복사 내용에서 제거한다.

### 4.2 준비 완료

첫 도우미 메시지:

> 게임과 연결됐어요. 지금 상태를 살펴보고 가장 필요한 것 하나를 알려드릴게요.

첫 선택지:

- `오늘 할 일 추천`
- `내 상태 쉽게 설명`
- `@@ 게임 채팅 사용법`

첫 브리핑은 하루 첫 실행 또는 중요한 변화 때 한 번만 나오며, 사용자가 먼저
입력하면 방해하지 않는다.

### 4.3 게임 연결 실패

| 상태 | 제목 | 설명 | 주 행동 |
|---|---|---|---|
| CLI 없음 | 게임 연결 도구를 찾지 못했어요 | 설치 경로를 자동으로 다시 찾을 수 있음 | 다시 찾기 |
| 게임 꺼짐 | 게임이 아직 실행되지 않았어요 | 게임 실행 뒤 자동으로 다시 확인 | 다시 확인 |
| 옵션 꺼짐 | 게임의 연결 기능이 꺼져 있어요 | 설정 위치를 그림과 함께 안내 | 설정 위치 보기 |
| 캐릭터 전 | 캐릭터 접속을 기다리고 있어요 | 로그인·캐릭터 선택·로딩 확인 | 다시 확인 |
| 응답 느림 | 게임 응답이 평소보다 늦어요 | 단절로 단정하지 않고 입력 보존 | 다시 확인 |
| 응답 형식 오류 | 게임 업데이트 확인이 필요해요 | 자동 실행을 멈추고 진단 제공 | 진단 정보 복사 |

### 4.4 AI 연결 실패

게임 상태와 분리해 `AI 대화만 잠시 사용할 수 없음`으로 표시한다. 로컬 기능은
계속 사용할 수 있음을 바로 아래에 보여준다. 인증 오류는 자동 재시도하지 않고
설정 화면으로 안내하며, 사용량 제한은 가능하면 다시 쓸 수 있는 시각을 보여준다.

AI 공급자는 `AIProvider` 인터페이스 뒤에 둔다. 첫 버전에서는 하나의 기본 공급자와
`AI 없이 사용`을 제공하고, 여러 공급자 선택과 키 관리 방식은 별도 설계·승인 뒤
추가한다. 자격증명 전체 값은 화면·로그·진단 정보에 표시하지 않는다.

### 4.5 장시간 작업

- 상단 `현재 작업`을 `진행 중`으로 바꾸고 작업 이름·시작 시각을 표시한다.
- 완료율을 알 수 없으면 가짜 진행률 대신 `게임에서 완료 응답을 기다리는 중`이라고
  쓴다.
- 검증 전에는 앱 취소 버튼을 두지 않고 `게임 안에서 중지해주세요`를 안내한다.
- 앱 닫기 요청 시 창을 숨기고 작업을 계속 기다리는 선택을 기본으로 한다.
- 이때 입력한 `@@`는 한 건만 대기시키며 수정·취소할 수 있다.

### 4.6 결과 불명 복구

`성공`이나 `실패`로 추측하지 않고 `결과 확인 중`을 표시한다. 게임이 다시 연결되면
작업 전후 상태를 비교하고 `완료 확인`, `실패 확인`, `여전히 알 수 없음` 중 하나로
정리한다. 상태 변경 명령은 자동 재실행하지 않는다.

## 5. 설정

초기 설정은 다음만 제공한다.

- AI 연결: 연결 상태, 공급자 설정, `AI 없이 사용`
- 제안: 전체 켜기, 오늘 조용히, 제안 종류별 켜기
- 게임 연결: CLI 경로, 자동 탐색, 진단 정보
- 접근성: 큰 글자, 동작 줄이기, 화면 읽기 알림 수준
- 개인정보: 저장되는 대화 범위, 진단 정보에 포함하지 않을 항목

고급 연결 주기와 내부 제한 시간은 일반 사용자 화면에 노출하지 않고 진단 모드에만
둔다.

## 6. 상태 조합 표시

| 게임 | AI | 작업 | 주 화면 |
|---|---|---|---|
| 준비 | 준비 | 없음 | 대화와 선제 제안 모두 사용 |
| 준비 | 오프라인 | 없음 | 로컬 기능·`@@` 유지, AI 복구 안내 |
| 끊김 | 준비 | 없음 | 일반 AI 대화 유지, 게임 기능 복구 안내 |
| 준비 | 준비 | 실행 중 | 작업 상태 우선, 조회와 채팅 대기 표시 |
| 재연결 | 관계없음 | 결과 불명 | 화해 진행을 최우선 표시 |

세 상태를 하나의 “연결 안 됨”으로 합치지 않는다.

## 7. 접근성과 반응형 기준

- 320px 너비부터 1,024px 데스크톱 폭까지 가로 스크롤 없이 재배치한다.
- 125%, 150%, 200% 배율에서 중요한 버튼과 문구가 잘리지 않는다.
- 모든 기능을 `Tab`, `Shift+Tab`, `Enter`, `Space`, `Esc`로 사용할 수 있다.
- 상태 변경은 `aria-live=polite`, 직접 해결이 필요한 오류만 `role=alert`로 알린다.
- 색상만으로 의미를 전달하지 않으며 명암 대비와 Windows 고대비를 확인한다.
- 동작 줄이기 설정에서는 반복·장식 애니메이션을 사용하지 않는다.
- 새 메시지가 생겨도 사용자가 읽던 위치와 입력 초점을 강제로 이동하지 않는다.

## 8. 화면 문구 원칙

- 실패 이유와 다음 행동을 한 문장씩 나눈다.
- `파이프`, `스키마`, `종료 코드` 같은 내부 용어는 진단 영역에서만 쓴다.
- “오류가 발생했습니다” 대신 “게임이 꺼져 있어 연결할 수 없어요”처럼 원인을 쓴다.
- 확인되지 않은 결과는 “완료됐어요”가 아니라 “완료 여부를 확인하고 있어요”라고 쓴다.
- 비용이 드는 행동은 실행 선택지에 비용을 함께 표시한다.

## 9. 목업 검토 시나리오

인터랙티브 목업은 다음 상태를 전환해 확인한다.

1. 준비 완료
2. 게임 꺼짐
3. 연결 옵션 꺼짐
4. AI 오프라인
5. 장시간 작업 진행 중
6. 결과 불명 복구 중

각 상태에서 상단 상태, 주 안내, 가능한 행동, 입력 가능 범위가 함께 바뀌어야 한다.

## 10. 승인 기준

- 처음 보는 사용자가 30초 안에 게임·AI·작업 상태를 각각 설명할 수 있다.
- 사용자가 일반 AI 대화와 `@@` 게임 채팅을 첫 시도에 구분한다.
- 게임과 AI 중 하나가 끊겨도 계속 쓸 수 있는 기능을 찾을 수 있다.
- 연결 복구, 장시간 작업, 결과 불명 상태에서 자동 재실행으로 오해하지 않는다.
- 키보드만으로 첫 선택지, 상태 해결, 입력, 전송, 설정에 도달한다.
- 200% 배율과 고대비에서 핵심 정보와 행동이 잘리지 않는다.
- 높은 중요도 UI 리뷰 문제 0건 뒤 구현 단계로 이동한다.

## 11. 변경 기록

| 날짜 | 상태 | 내용 |
|---|---|---|
| 2026-09-21 | 검토 대기 | 연결 설계에 맞춘 첫 UI 상세설계 작성 |
| 2026-09-21 | 변경 필요 | 서브에이전트 2인 독립 리뷰, 승인 보류 (`reviews/04-UI-상세설계-리뷰-1.md`) |
