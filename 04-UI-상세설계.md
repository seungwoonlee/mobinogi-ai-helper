# 모비노기 AI 도우미 — UI 상세설계

| 항목 | 내용 |
|---|---|
| 문서 상태 | 재검토 대기 |
| 작성일 | 2026-09-21 |
| 수정일 | 2026-09-21 |
| 대상 | AI 사용이 익숙하지 않은 마비노기 모바일 PC 이용자 |
| 제품 형태 | Windows용 대화형 도우미 앱 |
| 선행 문서 | `02-연결-상태-설계.md`, `03-명령-실행과-안전-설계.md`(둘 다 승인) |
| 관련 리뷰 | `reviews/04-UI-상세설계-리뷰-1.md` |
| 다음 관문 | 수정본 서브에이전트 재검토 → 인터랙티브 목업 리뷰 |

이 문서는 **목업 전용** 설계다. 구현은 02 §16의 기반 단계(2~6)가 끝난 뒤 7단계에서
시작한다. 화면의 상태·결과·승인 값은 02·03이 공급하며 이 문서는 그 값을 어떻게
보여주는가만 정한다. 시간·수치 후보값은 02·03의 `[후보]`를 따른다.

## 1. 제품 경험 목표

앱을 실행하면 사용자가 빈 화면에서 무엇을 물어볼지 고민하지 않도록 현재 게임 상태를
바탕으로 먼저 한 가지 도움을 제안한다. 사용자는 명령어를 외우지 않고 평소 말하듯
요청하며, 게임 채팅으로 보낼 때만 문장 앞에 `@@`를 붙인다.

핵심 원칙:

1. 첫 화면만 보고 게임 연결, AI 대화 가능 여부, 현재 작업 상태를 구분할 수 있다.
2. 기술 용어보다 "게임을 켜주세요", "게임 설정에서 MM AI 에이전트를 켜주세요"처럼
   다음 행동을 말한다.
3. 제안은 한 번에 하나만 강조하고 자동 실행하지 않는다. 앱이 자동으로 하는 것은
   **상태 확인뿐**이며 채팅·작업은 사용자가 확정해야 실행된다(도움말과 상태 카드에 이
   문장을 상시 노출한다).
4. 새 제안과 상태 갱신은 입력창 초점을 빼앗지 않는다.
5. AI가 없어도 연결 안내, 준비된 도움말, `@@` 채팅 같은 로컬 기능을 유지한다.

## 2. 용어 대응

화면 용어는 02·03의 상태·결과와 다음처럼 1:1 대응한다. 내부 용어는 화면에 쓰지 않는다.

| 화면 결과 용어 | 03 결과 | 02 상태 |
|---|---|---|
| 완료 | `COMPLETED` | `IDLE` 종결 |
| 완료(결과 확인 전) | `COMPLETED` + `reconcile_incomplete` | `RECONCILING` 실패 |
| 일부 완료 | `PARTIAL` | `IDLE` 종결 |
| 결과 확인 중 | `UNVERIFIED` | `UNKNOWN_OUTCOME` |
| 확인 불가 | `UNRESOLVED` | `IDLE` 종결 |
| 실패 | `FAILED` | `FAILED` → `IDLE` |

게임 안의 설정 이름은 **"MM AI 에이전트"**로 통일해 쓴다(README §1과 같다).

## 3. 입력 규칙

| 입력 예 | 처리 | 화면 표시 |
|---|---|---|
| `오늘 뭐 하지?` | AI에게 질문 | 도우미 답변과 선택지 |
| `가방 상태 알려줘` | AI가 게임 조회를 계획 | 조회 중 표시 후 쉬운 설명 |
| `@@안녕하세요` / `@@ 안녕하세요` | 게임 채팅(동일 처리) | 전송 전 미리보기 → `보내기` |
| `@@ 고마워요` | 게임 채팅, 뜻이 분명하면 행동 자동 추가 | 미리보기에 `고마워요 😍 (행동: /하트)` |
| `@@`만 입력, `@@ ` 뒤 공백만 | 전송하지 않음 | "보낼 말을 입력해주세요" |
| `@@ /지역`, `@@ #x` | 거부 | "슬래시(/)나 #로 시작하는 말은 보낼 수 없어요" |
| 여러 줄 붙여넣기 | 거부 | "한 줄만 보낼 수 있어요" |
| 붙여넣은 `@@` 문구 | 확인 필요 | "이 내용을 보낼까요?" |

- `@@문구`와 `@@ 문구`는 동일하다. `@@` 직접 입력의 게임 채팅에는 어울리는 이모지 하나가 붙는다.
  **정규화로 문구가 달라졌으면 정규화된 값**을 미리보기에 보여준다(03 §6.1).
- **`@@` 직접 입력에는 별도 확인창이 없지만 전송 전 미리보기가 있다.** 사용자가 키로
  직접 친 한 줄 문구를 `보내기` 또는 Enter로 확정하면 전송된다(03 §5.3). 붙여넣기·AI가
  채운 문구는 이 예외에서 제외되어 확인 또는 승인 카드를 거친다(03 §5.4).
- 사용자 입력은 최대 48자다. 이모지 2자가 자동으로 붙어 게임 한도 50자가 된다는 설명을
  글자 수 표시 옆 도움말에 둔다: "이모지가 붙어서 48자까지 쓸 수 있어요".
- 한글 입력기(IME)로 글자를 조합 중일 때의 Enter는 조합 확정이며 전송하지 않는다. 조합이
  끝난 뒤의 Enter가 전송이다(9장).
- AI 경로로 나가는 채팅에는 자동 이모지·행동을 붙이지 않는다(03 §6.3).

## 4. 앱 구조

### 4.1 상단 영역

- 제품명: `모비노기 AI 도우미`
- 독립 상태 3개: `게임`, `AI 대화`, `현재 작업`(02 세 축과 1:1)
- 상태 배지는 아이콘, 짧은 문구, 형태(모양)를 함께 쓰며 색상만으로 구분하지 않는다.
- 배지를 선택하면 한 줄 설명, 마지막 정상 확인 시각, 가능한 해결 행동을 펼친다.
- 상단 고정 문구: "앱이 자동으로 하는 건 상태 확인뿐이에요."(도움말 펼침)
- 우측에는 설정 하나만 둔다.

### 4.2 대화 영역

- 사용자와 도우미의 대화를 시간순으로 표시한다.
- 도우미 메시지는 설명 뒤에 최대 세 개의 선택지만 제시한다. 선택지를 눌러도 곧바로
  실행되지 않고 4.5의 승인 카드가 열린다(03 §5.2).
- 게임 상태에서 나온 내용에는 관찰 시각과 `최신`/`오래된 정보`를 표시한다. 스냅샷 품질(02 §11)이 `fresh`면 `최신`, `stale`이면 `오래된 정보`, `partial`은 "일부만 확인됐어요", `invalid`는
  "확인하지 못했어요"로 쓴다. `fresh`가 아닌 값은 제안 판단에 쓰지 않는다(02 §11).
- 연결 오류는 대화 메시지를 쌓지 않고 상단 상태 카드 하나를 갱신한다. 단, **사용자가 한
  질문이 연결 문제로 답을 못 받으면** 대화에 실패 응답을 남긴다(03 §7).
- 실행 결과는 2장의 결과 용어 5종(+ `완료(결과 확인 전)`)만 쓰고 4.6의 결과 카드로
  표시한다.

### 4.3 오늘의 제안

- 가장 중요한 제안 하나만 대화 상단에 고정한다. 우선순위: 긴급(복구 필요·결과 불명) >
  도움 > 참고 > 첫 실행 안내(02 §13). 상태 카드와 겹치면 상태 카드가 위에 온다.
- 기본 행동은 `자세히 보기`이며 상태 변경은 사용자가 다시 선택해야 한다. AI 없이 만든 **규칙 기반 제안**도 실행하려면 같은 승인 카드(4.5)를 거친다(03 §5.4).
- 첫 실행 안내는 02 §13의 등급이 아닌 온보딩 카드이며 다른 제안이 없을 때만 보인다.
- 보조 행동: `1시간 조용히`, `오늘은 숨기기`, `이 제안 끄기`. 긴급 제안에는 `1시간 조용히`·`오늘은 숨기기`를 제공하지 않으며 복구 행동만 앞에 둔다. `이 제안 끄기`도 긴급 등급에서는 제공하지 않는다.
- 조용히·숨김·끄기 설정은 재시작 뒤에도 유지한다.

### 4.4 입력 영역

- 한 줄 입력창과 `보내기` 버튼을 둔다.
- 입력이 `@@`로 시작하면(같은 판정 함수, 03 §6.1) 테두리·아이콘·안내 문구가 `게임 채팅`
  모드로 바뀐다. 그 외는 `AI에게 부탁하기` 모드다.
- 입력창 아래: `@@를 붙이면 게임 채팅으로 보내요` 도움말, 글자 수, 게임 채팅일 때의 미리보기 한 줄과 "다른 플레이어에게 보이는 게임 채팅이에요" 안내.
- 초과·거부·정규화는 텍스트로 이유를 알린다(3장).
- 전송 중에는 `보내기`와 Enter를 잠근다. 같은 문구를 60초 안에 다시 보내려 하거나 직전
  결과가 `확인 불가`이면 "방금 보낸 말과 같아요. 이미 전송됐을 수 있어요. 다시 보낼까요?"를
  묻는다(03 §5.4).
- 게임 연결이 확인되지 않는 동안에도 입력은 막지 않는다. `보내기`만 비활성화하고 이유를 보이며,
  입력한 `@@`는 초안으로 보존한다(6장).

### 4.5 승인 카드

AI가 제안한 실행(`ACTION`, AI 경로 `CHAT`·`BEHAVIOUR`)은 선택지를 누르면 카드가 열린다.
카드는 대화창 위에 뜨는 인라인 패널이며 입력 초점을 빼앗지 않는다(사용자가 선택지를
눌렀을 때만 열린다).

| 카드 항목 | 표시 |
|---|---|
| 제목 | 하는 일(`label`, 중개기가 생성) 예: "가방 물품 수거하기" |
| 보낼 곳·내용 | 채팅이면 "게임 채팅 — 다른 플레이어에게 보여요"와 보낼 문구 그대로. 작업이면 검증된 인자 |
| 비용 | 총비용 합계와 단위(명령 표가 정함). 다회 호출이면 "5번 나눠서 진행, 총 ○○ 25개". 모르면 "비용을 확인할 수 없어요" |
| 근거 시각 | "○분 전 정보 기준" + 오래됐으면 `오래된 정보` |
| 선택지 | `실행`, `취소`. 기본 초점은 `취소` |

- `실행`이 승인이다(하나의 작업 전체). 카드 표시 뒤 2분 `[후보]` 안에 눌러야 하며 지나면
  카드가 "시간이 지났어요. 다시 확인해주세요"로 바뀌고 한 번의 조작으로 다시 열 수 있다.
- 실행 직전 상태가 달라졌으면(연결·재화·인자) 카드가 다시 뜬다.
- 게임 축이 `READY`가 아니게 되면(연결 확인 필요 포함) 카드의 `실행`을 비활성화하고 "게임 연결을 확인하고 있어서 지금은 실행할 수 없어요"를 보인다. 연결이 돌아오면 다시 활성화한다.
- `Esc`는 `취소`와 같다. 카드가 닫히면 초점은 카드를 연 선택지로 돌아간다. 카드는 **사용자가 선택지를 눌러 연 경우에만** 초점을 가져가며(제목으로 이동, 기본 초점은 `취소`), 그 밖의 도착(제안·알림)은 초점을 옮기지 않는다.
- 승인한 뒤 곧바로 실행할 수 없으면(다른 작업 진행 중) 대화에 "실행을 기다리는 중이에요"와 `대기 취소`를 보인다. 실행 직전 상태가 달라졌으면 "상태가 달라져서 다시 확인이 필요해요"로 카드를 다시 열고, 30분 넘게 기다렸으면 "너무 오래 기다려서 취소됐어요"를 알린다(02 C7·C7a). 대기 중 게임 연결을 확인하는 상황이면 "게임 연결을 확인하고 있어서 대기 중이에요"를 보인다(C8).
- 대기 슬롯에 이미 작업이 있으면 카드 대신 "이미 대기 중인 작업이 있어요. 대기 작업을
  취소하고 바꿀까요?"를 보인다(`대기 취소 후 바꾸기`, `그대로 두기`).

### 4.6 결과 카드

작업 결과는 대화에 카드 하나로 남는다.

| 결과 | 제목 | 설명 | 다음 행동 |
|---|---|---|---|
| 완료 | 완료했어요 | 결과와 비용 | — |
| 완료(결과 확인 전) | 완료됐어요 | 결과 확인은 아직 못 했어요 | 없음(앱이 다시 확인하지 않아요). "게임에서 직접 확인해보세요" |
| 일부 완료 | 일부만 됐어요 | 채팅은 보냈고 행동만 실패, 또는 일부 호출만 성공. 채팅은 다시 보내지 않아요 | 행동 실패 확정일 때만 `행동만 다시 보내기`. 행동 결과가 불명이면 버튼 없이 "행동은 됐는지 알 수 없어요". 작업의 일부 호출 성공은 `새로 요청하기`(이미 쓴 비용과 이중 소모 위험 안내) |
| 결과 확인 중 | 실행 여부를 확인하고 있어요 | 자동으로 다시 실행하지 않아요. 경과 시간을 표시하고 "게임이 다시 연결되면 확인하고, 10분 안에 못 하면 '확인 불가'로 알려드려요"를 안내 | 없음(기다리기). "그동안 게임에서 직접 확인해도 돼요" |
| 확인 불가 | 결과를 확인할 수 없어요 | 채팅: "전송됐는지 확인할 수 없어요. 다시 보내지 않았어요. 게임 채팅창에서 확인해보세요". 작업: "게임에서 직접 확인해주세요" | 새로 요청하기(이중 소모 위험 안내 포함) |
| 실패 | 하지 못했어요 | 원인(게임이 꺼져 있거나 연결하지 못해서, 게임이 글자 수를 거부해서 등)과 다음 행동 | `새로 요청하기`(채팅은 입력창을 비우고 다시 입력, 작업은 승인 카드를 다시 거침). 원인별로는 실행하지 않는 행동만(`다시 확인`, `설정 위치 보기`) |

## 5. 상태별 화면 매핑

02·03의 모든 상태를 다음 표로 화면에 대응한다. `라이브`는 화면 읽기 프로그램에 알리는
수준이며 `알림`은 즉시 읽음, `정보`는 정중한 알림, `없음`은 알리지 않는다(9장).

### 5.1 게임 배지

| 02 상태 | 배지 문구 | 설명(한 줄) | 주 행동 | 채팅 보내기 | 라이브 |
|---|---|---|---|---|---|
| `CHECKING` | 확인 중 | 게임과 연결을 확인하고 있어요 | 기다리기 | 초안만 | 없음 |
| `CLI_MISSING` | 연결 도구 없음 | 게임과 이어주는 도구를 찾지 못했어요 | `다시 찾기`, `경로 직접 지정` | 초안만 | 정보 |
| `LOCAL_CONFIG_ERROR` | 설정 필요 | 도구 경로나 권한을 확인해야 해요 | `경로 직접 지정`, `진단 정보 복사` | 초안만 | 정보 |
| `GAME_UNAVAILABLE` | 게임 연결 안 됨 | 게임이 꺼져 있거나 연결하지 못했어요(원인 미확정이면 "원인을 알 수 없어요") | `다시 확인` | 초안만 | 정보 |
| `OPTION_OFF` | MM AI 에이전트 꺼짐 | 게임 설정에서 MM AI 에이전트를 켜야 해요 | `설정 위치 보기` | 초안만 | 정보 |
| `TRANSPORT_READY` | 확인 중 | 게임 준비 상태를 확인하고 있어요 | 기다리기 | 초안만 | 없음 |
| `SCHEMA_LOADING` | 준비 중 | 게임이 준비 중이에요 | 기다리기 | 초안만 | 없음 |
| `SESSION_CHECKING` | 확인 중 | 캐릭터 접속을 확인하고 있어요 | 기다리기 | 초안만 | 없음 |
| `SESSION_NOT_READY` | 캐릭터 대기 | 로그인·캐릭터 선택·로딩을 기다리고 있어요 | `다시 확인` | 초안만 | 정보 |
| `READY` | 연결됨 | 게임과 연결됐어요 | — | 가능 | 없음 |
| `READY` + `probe_paused_by_action` | 작업 중 | 작업 중이라 연결 확인을 멈췄어요. 마지막 정상 응답 ○분 전 | 기다리기 | 초안만 | 없음 |
| `RECONNECTING` | 연결 확인 중 | 연결을 다시 확인하고 있어요. 보이는 정보는 오래됐을 수 있어요 | `다시 확인` | 초안만 | 정보 |
| `SLOW_RESPONSE` | 응답 느림 | 게임 응답이 평소보다 늦어요(끊겼다고 단정하지 않아요) | `다시 확인` | 초안만 | 정보 |
| `PROTOCOL_ERROR` | 확인 필요 | 게임 또는 도구 버전이 맞지 않을 수 있어요. 자동 실행을 멈췄어요 | `다시 확인`, `진단 정보 복사` | 초안만 | 알림 |

"초안만"은 입력은 되고 `보내기`가 비활성이며 이유가 보이는 상태다(4.4, 6장).

### 5.2 AI 배지

| 02 상태 | 배지 문구 | 설명 | 주 행동 | 자유 질문 | 라이브 |
|---|---|---|---|---|---|
| `NOT_CONFIGURED` | AI 없이 사용 | AI를 연결하면 자유롭게 물어볼 수 있어요 | `AI 연결하기` | 불가(안내) | 없음 |
| `CHECKING` | 확인 중 | AI 연결을 확인하고 있어요 | 기다리기 | 대기 | 없음 |
| `READY` | 연결됨 | 물어보세요 | — | 가능 | 없음 |
| `OFFLINE` | 연결 안 됨 | 인터넷이나 서비스에 연결하지 못했어요. 준비된 도움말과 `@@`는 계속 쓸 수 있어요 | `다시 확인` | 불가 | 정보 |
| `AUTH_ERROR` | 인증 확인 필요 | 인증에 문제가 있어요. 자동으로 다시 시도하지 않아요 | `설정 열기` | 불가 | 알림 |
| `RATE_LIMITED` | 잠시 대기 | 사용량 제한이에요. ○시 ○분부터 다시 쓸 수 있어요 | 기다리기 | 불가 | 정보 |

요청 상태(03 §7): `AI_THINKING`은 대화에 "생각하는 중이에요"와 `중단`, `AI_TOOL_WAITING`은 "게임 정보를 확인하고 있어요"와 `중단`,
`AI_STREAMING`은 답이 흐르는 중과 `중단`, `AI_FAILED`는 "답을 받지 못했어요. 다시
물어볼까요?"와 `다시 묻기`, `AI_ABORTED`는 "중단했어요"로 표시한다.

### 5.3 작업 배지

| 02 상태 | 배지 문구 | 설명 | 주 행동 | 입력 | 라이브 |
|---|---|---|---|---|---|
| `IDLE` | 없음 | 진행 중인 작업이 없어요 | — | 모두 | 없음 |
| `RUNNING_QUERY` | 조회 중 | 정보를 확인하고 있어요(짧으면 배지를 깜박이지 않고 대화에만 표시) | 기다리기 | AI 질문 가능, `@@`는 초안 | 없음 |
| `RUNNING_SEND` | 전송 중 | 채팅을 보내고 있어요 | 기다리기 | 전송 잠금 | 없음 |
| `RUNNING_ACTION` | 진행 중 | `label` + 시작 시각. "게임에서 완료 응답을 기다리는 중이에요" | 게임 안에서 중지 안내 | AI 질문 가능, `@@`는 초안 | 정보 |
| `RECONCILING` | 정리 중 | 끝난 작업의 결과를 정리하고 있어요. 다시 실행하지 않아요 | 기다리기 | AI 질문 가능, `@@`는 초안 | 없음 |
| `UNKNOWN_OUTCOME` | 결과 확인 중 | 실행 여부를 확인하고 있어요. 자동으로 다시 실행하지 않아요 | 기다리기 | AI 질문 가능, `@@`는 초안 | 알림 |
| `FAILED` | 하지 못했어요 | 결과 카드 참조(일시 상태) | 결과 카드 행동 | 모두 | 알림 |
| (보조 칩) 대기 슬롯에 1건 | 대기 1건 | 다음에 실행할 작업이 기다리고 있어요. 위 배지와 함께 보조로 표시 | `대기 취소` | 모두 | 정보 |

`CANCELLING`은 1차 구현에서 사용하지 않으므로 화면이 없다(02 §4.3). `RUNNING_ACTION` 중 AI가 시작한 조회는 대기시키지 않고 최근 정보(관찰 시각 포함)를 보여주거나
"작업 중이라 잠시 뒤에 확인해요"로 답한다(02 C4a).

## 6. 상태 조합 표시

세 상태를 하나의 "연결 안 됨"으로 합치지 않는다. 아래 "게임 문제"는 게임 배지가 `연결됨`이 아닌 모든 경우(시작 중 진행 상태 포함)이고, "AI 문제"는 AI 배지가 `연결됨`·`AI 없이 사용`이 아닌 경우다. 표끼리 결과가 충돌하면 우선순위는 **결과 확인 중 > 승인 카드 > 게임 문제 > AI 문제 > 작업 진행 > 제안** 순이다.

주요 조합의 주 화면:

| 게임 | AI | 작업 | 주 화면 | 입력 가능 범위 |
|---|---|---|---|---|
| 연결됨 | 연결됨 | 없음 | 대화와 선제 제안 | 모두 |
| 연결됨 | 연결 안 됨·인증 필요·잠시 대기 | 없음 | 로컬 기능·`@@` 유지, AI 복구 안내 | `@@`, 준비된 도움말 |
| 연결됨 | AI 없이 사용 | 없음 | 규칙 기반 도움, `AI 연결하기` 안내 | `@@`, 선택지 |
| 게임 문제 | 연결됨 | 없음 | 일반 AI 대화 유지, 게임 복구 안내 | AI 질문(게임 조회는 "지금은 못 봐요"), `@@`는 초안 |
| 게임 문제 | AI 문제 | 없음 | 두 복구 안내를 나란히, 준비된 도움말 | `@@`는 초안 |
| 연결됨 | 연결됨 | 진행 중 | 작업 상태 우선, 조회는 최근 정보 | AI 질문, `@@`는 초안 |
| 연결 확인 중 | 무관 | 진행 중 | 게임 축은 마지막 정상 응답 시각을 보이고, 작업은 결과 확인 대기로 이어짐 | `@@`는 초안 |
| 연결됨 | 무관 | 결과 확인 중 | 결과 확인 중을 최우선 표시 | AI 질문, `@@`는 초안 |
| 연결됨 | AI 문제 | 진행 중 | 작업 상태와 AI 복구 안내를 함께, 작업이 우선 | `@@`는 초안, 준비된 도움말 |
| 연결됨 | AI 없이 사용 | 진행 중 | 작업 상태 우선 | `@@`는 초안, 선택지 |
| 게임 문제 | 무관 | 대기 슬롯 1건 | 대기 안내("게임 연결을 확인하고 있어서 대기 중이에요") | 대기 취소, `@@`는 초안 |
| 게임 문제 | 무관 | 승인 카드 열림 | 카드 `실행` 비활성과 이유 | 카드 `취소`, `@@`는 초안 |
| 게임 문제 | 무관 | 결과 확인 중 | 게임 복구 안내 + 결과 확인 대기 | `@@`는 초안 |
| 응답 느림·프로토콜 오류 | 무관 | 없음 | 상태 카드, 자동 실행 중지 안내 | `@@`는 초안, 조회는 안내 |

## 7. 핵심 화면

### 7.1 첫 실행

게임 연결 검사와 AI 설정은 **독립된 두 트랙**이며 서로를 막지 않는다.

- **게임 트랙(한 화면에서 순서대로)**: 1) 게임 찾기 2) 게임 실행 확인 3) 게임 설정의 MM AI
  에이전트 확인 4) 캐릭터 접속 확인 5) 준비 완료(02 §12). 완료된 단계는 접고 막힌 단계 하나만
  크게 보인다. 공통 행동: `다시 확인`, `설정 위치 보기`(그림과 대체 텍스트), `경로 직접 지정`,
  `진단 정보 복사`(개인정보·채팅 문구는 허용 목록으로 제한, 복사 전 미리보기).
- **AI 트랙(선택)**: 첫 화면에 `AI 연결하기`와 `AI 없이 시작`을 나란히 둔다. `AI 연결하기`는
  (1) 공급자 선택 (2) 자격증명 입력 (3) 연결 확인 3단계이며 언제든 건너뛸 수 있다(03 §7.2).
  AI로 나가는 정보 종류를 이 화면에서 한 번 알린다(03 §8). 자격증명 전체 값은 화면에 남기지
  않는다.
- 두 트랙은 병렬이다. AI를 설정하지 않아도 게임 트랙을 마치면 사용할 수 있다.

### 7.2 준비 완료

첫 도우미 메시지: "게임과 연결됐어요. 지금 상태를 살펴보고 가장 필요한 것 하나를 알려드릴게요."

첫 선택지 3개는 AI 없이도 동작한다.

| 선택지 | AI 있음 | AI 없음(규칙 문구, 03 §7.1) |
|---|---|---|
| `오늘 할 일 추천` | AI가 스냅샷을 바탕으로 제안 | "가방이 90% 찼어요. 정리부터 해볼까요?", "만복도가 낮아요. 음식을 먹어볼까요?", "미션이 3개 남았어요"처럼 스냅샷 값에 맞춘 고정 문장. 스냅샷이 없으면 "아직 게임 정보를 못 봤어요. 연결을 먼저 확인해볼까요?" |
| `내 상태 쉽게 설명` | AI가 풀어서 설명 | 체력·만복도·가방을 정해진 문장으로 표시 |
| `@@ 게임 채팅 사용법` | 준비된 도움말 | 준비된 도움말 |

첫 브리핑은 하루 첫 실행 또는 중요한 변화 때 한 번만 나오고 사용자가 먼저 입력하면
방해하지 않는다. 즉시 위험(체력 위험·가방 99% 등)은 최초 스냅샷이라도 한 번 알린다(02 §13).
AI 미설정 상태에서 자유 질문을 입력하면 "AI를 연결하면 자유롭게 물어볼 수 있어요. 지금은
준비된 도움말과 `@@` 채팅을 쓸 수 있어요"로 답하고 `AI 연결하기`를 제공한다.

### 7.3 게임 연결 실패

5.1 표가 문구를 정한다. 원인이 확정되지 않았으면 "원인은 확인 중이에요"를 덧붙이고 단정하지
않는다. 옵션 꺼짐은 처음 설정하는 경우와 사용 중 꺼진 경우를 나눈다: "게임 설정에서 MM AI
에이전트를 켜주세요" / "사용 중에 MM AI 에이전트가 꺼진 것 같아요. 다시 켜주세요". 도구를 찾지
못한 경우 설치 경로를 다시 찾는 행동과 직접 지정 행동을 함께 둔다. 입력 중인 문구는 모든
오류 상태에서 보존한다.

### 7.4 AI 연결 실패

게임 상태와 분리해 AI 배지만 바뀌고 "로컬 기능은 계속 쓸 수 있어요"를 바로 아래에 보인다
(5.2 표). AI 공급자 구현·키 관리는 별도 설계이며 이 문서는 AI 공급자 이름이나 구조를
정하지 않는다.

### 7.5 장시간 작업

- 작업 배지를 `진행 중`으로 바꾸고 작업 이름과 시작 시각, 경과 시간을 보인다.
- 완료율을 모르면 가짜 진행률 대신 "게임에서 완료 응답을 기다리는 중이에요"라고 쓴다.
- 앱 취소 버튼은 두지 않고 "게임 안에서 중지해주세요"를 안내한다(02 §8.2).
- 작업이 없을 때 창 닫기(X)는 앱을 종료한다. 종료 시 저장하지 않은 `@@` 초안이 있으면 "보내지 않은 말이 있어요. 종료하면 사라져요"를 알린다. 창을 숨기는 것은 작업이 진행 중일 때만이다.
- 앱 닫기 요청 시 "작업이 진행 중이에요"를 알리고 선택지를 준다: `창 숨기고 계속 기다리기`(기본),
  `그래도 종료`(종료하면 다음 실행에서 결과 확인 중으로 시작하고 자동으로 다시 실행하지
  않는다고 안내). 창이 숨겨지면 작업 표시줄 알림 영역 아이콘(이름 "모비노기 AI 도우미", 툴팁에 현재 게임·작업 상태 요약, 키보드로 열기 가능)으로 다시 연다. 운영체제 알림은 설정에서 끌 수 있다.
- 완료·대기 문구 발송 가능은 운영체제 알림으로 알린다. 알림에는 채팅 문구를 넣지 않는다.
- 진행 중 프로세스가 멈춘 것 같아도 앱이 임의로 끊지 않는다(02 §8.2). 멈춘 것 같으면 "게임에서
  직접 확인해주세요"를 안내한다.

### 7.6 결과 확인 중과 복구

`성공`이나 `실패`로 추측하지 않는다. 게임이 다시 연결되면 작업 전후 상태를 비교해 4.6의 결과
카드(완료 / 일부 완료 / 실패 / 확인 불가) 중 하나로 정리한다. 채팅은 비교할 수 없으므로
곧바로 `확인 불가`가 된다(03 §4). 앱을 다시 시작했을 때 미완료 컨텍스트가 있으면 첫 화면 위에 카드가 뜨고 작업 종류와 시각만 보인다(채팅 문구는 저장하지 않는다). **채팅·행동은 곧바로 `확인 불가` 카드**("전송됐는지 확인할 수 없어요. 게임 채팅창에서 확인해보세요")이고, **작업(`ACTION`)만 `결과 확인 중` 카드**다(02 C30·C31).

### 7.7 대기 초안

`@@`를 입력했지만 지금 보낼 수 없을 때(작업 중·게임 문제·결과 확인 중·조회/전송 중)의 초안:

- 입력창 위에 초안 카드: 문구, 입력한 시각, 보낼 수 없는 이유.
- 조건이 풀리면 "지금 보낼까요?" 알림과 `보내기` 버튼. **자동 발송하지 않는다.**
- 입력한 지 10분 `[후보]`이 지났으면 문구를 다시 보여주고 확인받는다.
- 새 `@@`를 입력하면 "대기 중인 말을 바꿀까요?"로 묻는다.
- 창이 숨겨졌으면 운영체제 알림으로 알리되 문구는 넣지 않는다.
- 앱을 종료하면 초안은 사라진다(입력 보존은 실행 중 범위).

## 8. 설정

초기 설정은 다음만 제공한다.

- AI 연결: 상태(인증은 "AI 서비스에서 받은 키"처럼 쉬운 말로 안내), `AI 연결하기`, `AI 없이 사용`, 설정 삭제
- 제안: 전체 켜기, 오늘 조용히, 제안 종류별 켜기(재시작 뒤 유지)
- 게임 연결: 연결 도구 경로, 자동 탐색, 진단 정보(허용 목록만, 복사 전 미리보기, 사용자 폴더 경로는 `%USERPROFILE%`로 바꾸고 게임 응답 본문은 제외). 진단 화면에 두 가지 한계를 적는다: "이 앱 밖에서 실행하는 연결 도구 호출은 막을 수 없어요", "보낸 채팅 문구는 연결 도구에 인코딩되어 전달되어 같은 PC의 다른 프로그램에서 볼 수 있을 수 있어요". 응답 원문은 진단 모드에서 마스킹해 표시
- 접근성: 큰 글자, 동작 줄이기, 화면 읽기 알림 수준
- 개인정보: 대화 기록은 기본으로 실행 중에만 보관하고 종료하면 지운다. 저장은 켰을 때만이며 저장 범위와 삭제 방법을 보인다
- 채팅: 자동 행동 켜기/끄기(기본 켬)
- 알림: 운영체제 알림 켜기/끄기(문구는 항상 넣지 않음)

고급 연결 주기와 내부 제한 시간은 일반 사용자 화면에 노출하지 않고 진단 모드에만 둔다.
"진단 정보에 포함하지 않을 항목" 같은 차단 목록 설정은 두지 않는다. 진단 정보는 항상 허용
목록(앱·CLI 버전, 상태, 종료 코드, 시각)만 담는다.

## 9. 접근성과 반응형

Windows 앱이므로 접근성은 UI 자동화(UIA) 기준으로 정의한다.

- 창 최소 폭 320px부터 1,024px 데스크톱 폭까지 가로 스크롤 없이 재배치한다.
- 배율 100·125·150·200%에서 핵심 행동이 잘리지 않는다. 글자만 키운 경우와 운영체제 큰 글자
  설정도 같다.
- 모든 기능을 `Tab`, `Shift+Tab`, `Enter`, `Space`, `Esc`로 쓸 수 있다. 탭 순서는 설정 → 상단 상태 → 오늘의 제안 → 대화 → 초안·결과·승인 카드 → 입력창 → `보내기`이며, 앱을 열면 초기 초점은 입력창이다. 포커스 표시는 두께 2px 이상 대비 3:1 이상이다.
- `Enter`는 조합이 끝난 뒤에 전송이다. 한글 조합 중 Enter는 조합 확정이며 전송하지 않는다.
  줄바꿈은 지원하지 않는다(한 줄 입력).
- **UIA 알림**: 상태 배지는 이름(예: "게임: 연결됨")과 상태 값을 노출한다. 5장의 `알림`은 UIA LiveSetting Assertive(즉시 읽음), `정보`는 Polite(정중하게 읽음), `없음`은 Off에 대응한다(운영체제 알림과는 별개다). 대화 목록은 List, 승인·초안·결과 카드는 제목과 역할(그룹)을 노출한다. 결과 카드는 `확인 불가`·`실패`가 Assertive, 나머지는 Polite이며 초안의 "지금 보낼까요?"는 Polite다. AI 답변은 토큰마다 읽지 않고 **완료 시 한 번**만 읽는다. 같은 상태가 흔들려도 새 알림을 쌓지 않고 마지막
  상태 하나만 갱신하며, 상태 변경 알림은 1초 이내 반복하지 않는다 `[후보]`.
- 승인 카드는 사용자가 선택지를 눌렀을 때만 열리며 그때 카드 제목으로 초점이 이동하고 `Esc`로 닫으면 원래 초점으로 돌아간다(4.5). 카드가 열려도 입력창의 내용과 커서는 유지된다. 새 메시지나 제안이 와도 사용자가 읽던
  위치와 입력 초점을 강제로 옮기지 않는다.
- 색상만으로 의미를 전달하지 않는다. 본문 대비 4.5:1 이상, 아이콘·경계 3:1 이상, Windows 고대비
  테마(어두움·밝음)를 확인한다. 터치·클릭 대상은 최소 44×44px다(03 §9.1). 길이 단위는 배율 적용 전 논리 픽셀(DIP)이다.
- 동작 줄이기는 운영체제 설정을 기본값으로 따르며 반복·장식 애니메이션을 쓰지 않는다.
- 설정 위치 그림에는 대체 텍스트를 넣고 게임 버전에 따라 그림이 바뀌면 텍스트 안내가 그림에
  의존하지 않는다.

## 10. 화면 문구 원칙

- 실패 이유와 다음 행동을 한 문장씩 나눈다.
- `파이프`, `스키마`, `종료 코드`, `화해` 같은 내부 용어는 진단 영역에서만 쓴다.
- "오류가 발생했습니다" 대신 원인을 쓴다. 단, 확인되지 않은 원인은 단정하지 않는다("게임이
  꺼져 있거나 연결하지 못했어요").
- 확인되지 않은 결과는 "완료됐어요"가 아니라 "실행 여부를 확인하고 있어요"라고 쓴다.
- 비용이 드는 행동은 실행 선택지에 총비용을 함께 표시한다.
- 작업 이름(`label`)은 중개기가 명령 표에서 만든 한국어 이름을 쓴다: 예 "가방 물품 수거", "재료
  채집 5회". 표는 구현 2단계에서 명령별로 작성한다.

## 11. 목업 검토 시나리오

인터랙티브 목업은 다음 상태를 전환해 확인한다. 각 상태에서 상단 3개 배지, 주 안내, 가능한 행동,
입력 가능 범위, 라이브 수준이 5·6장 표와 일치해야 한다.

1. 첫 실행(게임 트랙 + AI 트랙 독립)
2. 준비 완료(AI 있음 / AI 없음)
3. 게임 배지 13개 상태 각각과 작업 중(`probe_paused_by_action`)
4. AI 배지 6개 상태, AI 요청 실패·중단(R6·R7, T24·T47), 미설정에서 자유 질문
5. 장시간 작업 진행 중과 앱 닫기 요청
6. 결과 확인 중과 재시작 뒤 복구
7. 승인 카드(정상·만료·게임 끊김·다회 호출·비용 미상·오래된 정보)
8. `@@` 미리보기·채널 안내, 자동 행동, 붙여넣기 확인, 중복 확인, 정규화 표시, 진단 복사 미리보기
9. 대기 초안(조건 해소·10분 경과·교체·창 숨김 알림)과 대기 슬롯(대기·재검증 실패·30분 만료)
10. 결과 카드 6종(완료, 완료(결과 확인 전), 일부 완료, 결과 확인 중, 확인 불가, 실패)과 재시작 뒤 복구 카드 2종
11. 200% 배율·Windows 고대비·키보드만·화면 읽기 변형

## 12. 승인 기준

- 03 §9.1의 사용성 시험 과업 7개와 접근성 수치를 이 화면에 적용해 확인한다. 시험은 문제 발견용이며
  과업당 5명 중 4명 미만 성공은 개선 대상이다.
- 사용자가 일반 AI 대화와 `@@` 게임 채팅을 첫 시도에 구분한다(과업 2).
- 연결 복구·장시간 작업·결과 불명 상태에서 자동 재실행으로 오해하지 않는다(과업 6).
- 게임과 AI 중 하나가 끊겨도 계속 쓸 수 있는 기능을 찾는다(과업 7).
- 키보드만으로 첫 선택지, 상태 해결, 입력, 전송, 승인 카드, 설정에 60초 안에 도달한다.
- 200% 배율과 고대비에서 핵심 정보와 행동이 잘리지 않는다. **핵심 정보·행동**은 세 상태 배지, 주 행동 버튼, 입력창과 `보내기`, 승인 카드의 내용과 `실행`·`취소`, 결과 카드다.
- 5장·6장의 모든 상태와 4.6의 결과 카드 6종이 목업에서 확인된다(11장 시나리오 11개).
- 목업은 실제 CLI를 호출하지 않고 성공 목록을 채운 모의 CLI만 쓴다(03 §9.1).
- 서브에이전트 리뷰에서 높은 중요도 문제 0건이고 `00-로드맵.md`에 승인 기록이 남은 뒤 구현 단계(02 §16의 7단계)로 이동한다.

## 13. 변경 기록

| 날짜 | 상태 | 내용 |
|---|---|---|
| 2026-09-21 | 검토 대기 | 연결 설계에 맞춘 첫 UI 상세설계 작성 |
| 2026-09-21 | 변경 필요 | 서브에이전트 2인 독립 리뷰, 승인 보류 (`reviews/04-UI-상세설계-리뷰-1.md`) |
| 2026-09-21 | 변경 필요 | 2차 리뷰 2인, 승인 보류(높음 각 1건) |
| 2026-09-21 | 재검토 대기 | 입력 열 정정, 다시 시도 → 새로 요청하기, 채널·슬롯·복구 카드·접근성 UIA 정리 반영 |
| 2026-09-21 | 재검토 대기 | 02·03 승인 기준으로 전면 개정: 상태별 화면 매핑, 승인 카드, 결과 카드, 대기 초안, 첫 실행 AI 트랙, AI 요청 상태, UIA 접근성, 용어 대응 |
