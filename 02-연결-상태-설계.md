# 마비노기모바일 AI도우미 — 연결 상태 설계

| 항목 | 내용 |
|---|---|
| 문서 상태 | 재검토 대기 |
| 작성일 | 2026-09-21 |
| 수정일 | 2026-09-21 |
| 구현 상태 | 시작 전 |
| 관련 리뷰 | `reviews/02-연결-상태-설계-리뷰-1.md`, `reviews/02-03-기능설계-리뷰-2.md` |
| 짝 문서 | `03-명령-실행과-안전-설계.md` |
| 다음 관문 | 02·03 수정본 서브에이전트 재검토 |

## 1. 목표와 원칙

마비노기 모바일, 대화용 AI, 현재 게임 작업의 상태를 서로 독립적으로 관리한다.
사용자는 기술적인 파이프나 종료 코드를 몰라도 현재 가능한 기능과 해결 방법을
이해할 수 있어야 한다.

설계 원칙:

1. 게임 명령은 공용 CLI 어댑터와 명령 중개기를 통해서만 실행한다.
2. 조회와 상태 변경 명령의 재시도 정책을 분리한다.
3. 장시간 작업을 앱이 먼저 강제 종료하지 않는다.
4. 연결 복구 뒤에는 실행 결과를 추측하지 않고 게임 상태와 화해한다.
5. AI 서비스가 없어도 연결 안내, 로컬 규칙, `@@` 채팅은 가능한 범위에서 유지한다.
6. 선제 제안은 실행하지 않고 사용자의 선택을 기다린다.

## 2. 범위와 제외 범위

포함:

- CLI 탐색과 응답 검증
- 게임 연결·캐릭터 세션·AI 연결·명령 실행 상태 관리
- 재연결과 결과 불명 복구
- 조회 스케줄과 변화 감지
- 첫 실행·오류 해결·복구 사용자 흐름
- 알림 피로 방지와 접근성 요구
- 모의 CLI 기반 테스트 계획

현재 제외:

- 실제 Windows GUI 코드
- AI 공급자·과금·로그인 구현
- 채집·제작·변형 자동 실행
- 설치 프로그램과 자동 업데이트

## 3. 현재 상태와 확인 근거

현재 프로그램은 `@@` 채팅 전송 시점에만 CLI를 직접 실행한다. 공용 중개기,
백그라운드 연결 감지, 재연결, 스냅샷, 선제 제안은 아직 없다.

확인된 사실:

| 확인일 | 항목 | 결과 | 근거 |
|---|---|---|---|
| 2026-09-21 | `status` 정상 응답 | `{"pipe":"connected"}`, 종료 코드 0 | 실제 로컬 호출 |
| 2026-09-20 | 명령 목록 | 전체 28개, 게임 기능 27개 | 실제 `capabilities` 호출 |
| 2026-09-20 | 채팅 안전 메타데이터 | `requiresConfirm`이 문자열 `"true"`로 반환 | 실제 스키마 |

아직 실제 응답으로 확인하지 못한 값은 `[가정]`, 실측 전 임시 수치는 `[후보]`로 표시하고 §18에 항목을 둔다. 아직 확인하지 못한 것에는 코드 5 응답 본문으로 `game_off`·`option_off`를 구분할 수 있는지, `capabilities`의 분류·비용 메타데이터, `retryAfterSeconds` 필드가 있다. 구현 시 개인정보를
제거한 응답 표본과 CLI 버전을 테스트 자료로 저장한다.

## 4. 세 개의 독립 상태 축

### 4.1 게임 연결 상태 `GameConnectionState`

| 상태 | 의미 |
|---|---|
| `CHECKING` | CLI 위치와 게임 연결 확인 중 |
| `CLI_MISSING` | CLI를 찾지 못함 |
| `GAME_UNAVAILABLE` | 게임 미실행 또는 세부 원인을 확정할 수 없는 연결 실패 |
| `OPTION_OFF` | 응답 코드로 AI 에이전트 옵션 꺼짐이 확인됨 |
| `TRANSPORT_READY` | 파이프 연결은 됐으나 플레이 세션 확인 전 |
| `SCHEMA_LOADING` | 명령 목록 준비 중 |
| `SESSION_CHECKING` | 캐릭터 세션 확인 중 |
| `SESSION_NOT_READY` | 로그인·캐릭터 선택·로딩 등 캐릭터 명령 실행 전 |
| `READY` | 캐릭터 조회·명령 실행 가능 |
| `RECONNECTING` | 일시 오류 재확인 중 |
| `SLOW_RESPONSE` | 짧은 조회가 제한 시간을 넘겼으나 단절 확정 전 |
| `PROTOCOL_ERROR` | JSON 또는 명령별 응답 계약 불일치 |
| `LOCAL_CONFIG_ERROR` | 경로·권한·설정 오류 |

### 4.2 AI 연결 상태 `AIConnectionState`

| 상태 | 의미 | 로컬 대체 동작 |
|---|---|---|
| `NOT_CONFIGURED` | AI 공급자 미설정 | 준비된 문구와 로컬 규칙 |
| `CHECKING` | 연결 확인 중 | 기존 화면 유지 |
| `READY` | 대화 가능 | 전체 대화 기능 |
| `OFFLINE` | 네트워크·서비스 연결 실패 | 로컬 기능만 유지 |
| `AUTH_ERROR` | 인증 실패 | 설정 안내, 자동 재시도 안 함 |
| `RATE_LIMITED` | 사용량 제한 | 재시도 가능 시각과 기본 안내 |

### 4.3 명령 상태 `CommandState`

| 상태 | 의미 |
|---|---|
| `IDLE` | 실행 중인 명령 없음 |
| `RUNNING_QUERY` | `QUERY` 짧은 조회 실행 중 |
| `RUNNING_SEND` | `CHAT`·`BEHAVIOUR` 짧은 전송 중. 정기 조회 폐기와 전체 스냅샷을 일으키지 않음 |
| `RUNNING_ACTION` | `ACTION` 장시간 또는 상태 변경 명령 실행 중 |
| `CANCELLING` | 확인된 취소 절차 진행 중. **1차 구현에서는 사용하지 않음**(앱 취소를 제공하지 않으므로 도달 불가, §8.2) |
| `RECONCILING` | 작업 후 실제 게임 상태와 결과 화해 중 |
| `UNKNOWN_OUTCOME` | 실행 여부를 확정할 수 없음 |
| `FAILED` | 실패가 확정됨 |

### 4.4 용어 대응과 부가 값

| 02 상태 | 03 결과 | 화면 용어(04) |
|---|---|---|
| `RECONCILING` | `COMPLETED` 직전 확인 | 완료(확인 중 표시 없음). 화해 실패 시 `COMPLETED` + `reconcile_incomplete` |
| `UNKNOWN_OUTCOME` | `UNVERIFIED` | 결과 확인 중 |
| `IDLE` 종결 | `COMPLETED`, `PARTIAL`, `UNRESOLVED` | 완료 / 일부 완료 / 확인 불가 |
| `FAILED` | `FAILED` | 실패 |

게임 축에는 원본 상태 외에 부가 값 `probe_paused_by_action`(장시간 작업 중이라 연결
확인을 멈춤)과 `last_ok_at`(마지막 정상 응답 시각)을 둔다. UI는 작업 중에는 게임 축을
`READY`로 단정하지 않고 "작업 중이라 확인을 멈췄어요, 마지막 정상 응답 ○분 전"을
표시할 수 있다.

**대기 슬롯 `PendingSlot`**은 `CommandState`와 별개의 값(0 또는 1건)이다. 명령이 실행 중이거나 결과 확인 중일 때 도착한 **승인된 `ACTION`·AI 조회**를 보관한다. `@@` 초안은 슬롯이 아니라 03 §6.5의 초안이다. `QUEUED`라는 명령 상태는 두지 않는다.

`DEGRADED`는 원본 상태가 아니다. UI가 세 상태 축을 조합해 “게임은 연결됐지만
AI 대화는 사용할 수 없음”처럼 파생 표시한다.

## 5. 상태 전이

전이표는 세 축 각각의 **모든 상태가 들어가는 조건과 나가는 조건을 갖도록** 작성한다.
표에 없는 사건은 상태를 바꾸지 않고 진단에 기록한다. 각 행의 ID(G·A·C)는 테스트
시나리오 ID이며(§15) 행마다 모의 CLI 테스트를 최소 1건 둔다. "`[가정]`"은 실제 응답으로
확인되지 않은 조건이다(§18). 예외로 명령 상태 `CANCELLING`은 1차 구현에서 사용하지
않으므로 전이를 두지 않는다.

**세대 번호 규칙**: 세대 번호는 `QUERY`와 연결 확인 응답에만 적용한다. 진행 중이던
`ACTION`·`CHAT`의 **완료 응답은 세대와 무관하게 수용**한다.

### 5.1 게임 연결 상태

| ID | 현재 | 사건·조건 | 다음 | 부수 효과 |
|---|---|---|---|---|
| G1 | 앱 시작 | CLI 탐색 시작 | `CHECKING` | 요청 세대 번호 생성 |
| G2 | `CHECKING` | CLI 없음 | `CLI_MISSING` | 경로 지정·재확인 제공 |
| G3 | `CHECKING` | 경로·권한 오류 | `LOCAL_CONFIG_ERROR` | 설정 안내 |
| G4 | `CHECKING` | 코드 5 + `game_off` `[가정]` | `GAME_UNAVAILABLE` | 백오프 시작 |
| G5 | `CHECKING` | 코드 5 + `option_off` `[가정]` | `OPTION_OFF` | 설정 위치 안내 |
| G6 | `CHECKING` | 코드 5이나 원인 구분 불가 | `GAME_UNAVAILABLE`(원인 미확정 표시) | 백오프 시작 |
| G7 | `CHECKING` | `pipe=connected` | `TRANSPORT_READY` | 스키마 검증 예약 |
| G8 | `CHECKING` | 시간 초과·일시 실패 | `CHECKING` 유지(백오프 재시도) | 3회 또는 15초 `[후보]` 지속 시 G6 |
| G9 | `TRANSPORT_READY` | 스키마 캐시 유효 | `SESSION_CHECKING` | 세션당 스키마 재사용 |
| G10 | `TRANSPORT_READY` | 캐시 없음·무효 | `SCHEMA_LOADING` | 임시 파일 검증 후 교체 |
| G11 | `SCHEMA_LOADING` | 최상위 `loading=true` | 유지 | 2·5·10·30초 백오프. 5분 `[후보]` 지속 시 G13 |
| G12 | `SCHEMA_LOADING` | 스키마 검증 성공 | `SESSION_CHECKING` | 스키마 저장 |
| G13 | `CHECKING`·`TRANSPORT_READY`·`SCHEMA_LOADING`·`SESSION_CHECKING`·`READY`·`RECONNECTING`·`SLOW_RESPONSE`·`SESSION_NOT_READY` | 응답 계약 불일치(JSON 배열·필수 필드 없음 등) 또는 `loading` 5분 초과 | `PROTOCOL_ERROR` | 자동 실행 중지·진단 |
| G14 | 위 G13의 여덟 상태 | CLI 삭제 | `CLI_MISSING` | 다시 찾기 |
| G15 | 위 G13의 여덟 상태 | 경로·권한 오류 | `LOCAL_CONFIG_ERROR` | 설정 안내 |
| G16 | 위 G13의 여덟 상태 | 코드 5 + `game_off` `[가정]`·원인 미확정 | `GAME_UNAVAILABLE`(미확정이면 표시) | 안내 |
| G17 | 위 G13의 여덟 상태 | 코드 5 + `option_off` `[가정]` | `OPTION_OFF` | 설정 위치 안내 |
| G18 | `TRANSPORT_READY`·`SCHEMA_LOADING`·`SESSION_CHECKING`·`SESSION_NOT_READY`·`SLOW_RESPONSE` | 파이프 끊김, 또는 시간 초과(`SLOW_RESPONSE`는 G29 규칙 우선) | `RECONNECTING` | 백오프 |
| G19 | `SESSION_CHECKING` | 캐릭터 조회 거부 | `SESSION_NOT_READY` | 캐릭터 접속 안내 |
| G20 | `SESSION_CHECKING` | `get_my_info`·`get_activity` 유효 | `READY` | 기준 스냅샷 수집 |
| G21 | `SESSION_NOT_READY` | 백오프 재확인에서 세션 유효 | `READY` | 기준 스냅샷 수집 |
| G22 | `READY` | 첫 일시 실패(시간 초과 아님) | `RECONNECTING` | 기존 값은 오래됨 표시 |
| G23 | `READY` | 짧은 조회 시간 초과 | `SLOW_RESPONSE` | 큐 누적 금지·수동 재확인 제공 |
| G24 | `READY` | 장시간 작업 시작 | `READY` 유지, `probe_paused_by_action=true` | 다른 CLI 호출 중지, 프로세스만 감시 |
| G25 | `READY` | `RUNNING_ACTION`에서 나가는 **모든 전이**(C18~C23) | `READY` 유지, `probe_paused_by_action=false` | 즉시 연결 확인 1회, 실패 시 G22 |
| G26 | `RECONNECTING` | 정상 응답 1회 | `SESSION_CHECKING` | 세션 재확인(캐릭터 상태 변경 대비) |
| G27 | `RECONNECTING` | 실패가 3회 또는 15초 지속 `[후보]` | `GAME_UNAVAILABLE`(원인 미확정) | 백오프 |
| G28 | `SLOW_RESPONSE` | 다음 조회 정상 | `READY` | 안내 해제 |
| G29 | `SLOW_RESPONSE` | 시간 초과가 3회 또는 15초 지속 `[후보]` | `RECONNECTING` | 연결 불안정 확정 |
| G30 | `GAME_UNAVAILABLE`·`OPTION_OFF`·`CLI_MISSING`·`LOCAL_CONFIG_ERROR` | 백오프 재확인 또는 `다시 확인` | `CHECKING` | 안내 유지 후 결과로 갱신 |
| G31 | `PROTOCOL_ERROR` | 사용자 `다시 확인`, 또는 CLI·스키마 변경 감지 | `CHECKING` | 스키마 캐시 폐기 |
| G32 | 절전 복귀·시스템 시간 변경(`RUNNING_ACTION` 아님) | 사건 발생 | `CHECKING` | 모든 스냅샷 `stale`, 세대 번호 증가 |
| G33 | `RUNNING_ACTION` 중 절전 복귀 | 사건 발생 | 게임 축 유지 | 프로세스 생존만 확인, 완료 응답 수용 |
| G35 | `READY` | 조회가 캐릭터 미접속(세션 없음) 응답 | `SESSION_NOT_READY` | 기존 값은 오래됨 표시, 캐릭터 접속 안내 |
| G34 | 모든 상태 | 최신 세대보다 오래된 `QUERY` 응답 도착 | 상태 유지 | 응답 폐기·진단 기록 |

`GAME_UNAVAILABLE`은 "원인 미확정"을 표시할 수 있다. 실제 오류 코드로 로그인·캐릭터
선택 상태를 안정적으로 구분하지 못하면 `SESSION_NOT_READY` 하나로 묶고 복합 안내를
제공한다. G13~G17의 "여덟 상태"는 G13 행에 나열된 상태 집합이다.

### 5.2 AI 연결 상태

| ID | 현재 | 사건·조건 | 다음 | 부수 효과 |
|---|---|---|---|---|
| A1 | 앱 시작 | 공급자 설정 없음 | `NOT_CONFIGURED` | `AI 없이 사용` 범위(03 §7.1) |
| A2 | 앱 시작 | 설정 있음 | `CHECKING` | 연결 확인 |
| A3 | `NOT_CONFIGURED` | 사용자가 설정 완료 | `CHECKING` | 연결 확인 |
| A4 | `CHECKING` | 확인 성공 | `READY` | 전체 대화 기능 |
| A5 | `CHECKING`·`OFFLINE` | 네트워크·서비스 실패 | `OFFLINE` | 로컬 기능 유지, 백오프 재확인 |
| A6 | `CHECKING`·`READY`·`OFFLINE` | 인증 실패 | `AUTH_ERROR` | 설정 안내, 자동 재시도 안 함 |
| A7 | `CHECKING`·`READY`·`OFFLINE` | 사용량 제한 응답 | `RATE_LIMITED` | 재시도 가능 시각 표시 |
| A8 | `READY` | 요청 중 네트워크 실패 | `OFFLINE` | 요청은 03 §7의 `AI_FAILED` |
| A9 | `OFFLINE` | 백오프 재확인 성공 | `READY` | 안내 해제 |
| A10 | `RATE_LIMITED` | 재시도 가능 시각 도래 | `CHECKING` | 재확인 |
| A11 | `AUTH_ERROR` | 사용자가 자격증명 수정 | `CHECKING` | 재확인 |
| A12 | 모든 상태 | 사용자가 설정 삭제 | `NOT_CONFIGURED` | 저장된 자격증명 폐기 |
| A13 | `READY` | 요청이 무응답·제한 시간 초과(03 §7의 `AI_FAILED`) | `READY` 유지 | 연속 3회 실패 시 A8과 같이 `OFFLINE` |

### 5.3 명령 상태

`FAILED`는 결과를 기록하는 순간의 **일시 상태**이며 곧바로 `IDLE`로 간다. 사용자 결과
알림은 화면에 남지만 새 명령을 막지 않는다(`ACTION`·`CHAT` 동일).

| ID | 현재 | 사건·조건 | 다음 | 부수 효과 |
|---|---|---|---|---|
| C1 | `IDLE` | `QUERY` 시작 | `RUNNING_QUERY` | 제한 시간 시작 |
| C2 | `IDLE` | `CHAT`·`BEHAVIOUR` 시작(승인됨) | `RUNNING_SEND` | 단계 write-ahead 기록, 제한 시간 |
| C3 | `IDLE` | `ACTION` 시작(승인됨) | `RUNNING_ACTION` | 컨텍스트 저장 |
| C4 | `RUNNING_*`·`RECONCILING`·`UNKNOWN_OUTCOME` | 승인된 `ACTION`·`CHAT`·`BEHAVIOUR`(AI 경로) 도착, 슬롯 비어 있음 | 상태 유지 | `PendingSlot`에 보관, 대기 이유 표시 |
| C4a | `RUNNING_ACTION`·`RECONCILING`·`UNKNOWN_OUTCOME` | AI가 시작한 `QUERY` 도착 | 상태 유지 | 슬롯을 쓰지 않음. 최근 `fresh` 스냅샷과 관찰 시각을 보여주거나 "작업 중이라 잠시 뒤에 확인해요"로 안내(§7). 다른 CLI 호출은 보내지 않음 |
| C5 | 같은 상태들 | 슬롯이 이미 차 있음 | 상태 유지 | 새 요청 거부, "이미 대기 중인 작업이 있어요. 대기 작업을 취소하고 바꿀까요?" (`대기 취소 후 바꾸기` 제공) |
| C6 | `IDLE` 전환 시 슬롯 있음 | 실행 직전 재검증 통과 | 해당 `RUNNING_*` | 슬롯 비움 |
| C7 | `IDLE` 전환 시 슬롯 있음 | 재검증 실패, 또는 슬롯 대기가 30분 `[후보]` 초과 | `IDLE` | 슬롯 비우고 승인 카드 다시 표시(03 §5.2) |
| C8 | 슬롯 보관 중 | 게임 단절 | 슬롯 유지 | `실행` 비활성, 이유 표시 |
| C9 | 슬롯 보관 중 | 사용자가 취소 | 슬롯 비움 | — |
| C10 | `RUNNING_QUERY` | 정상 응답 | `IDLE` | — |
| C11 | `RUNNING_QUERY` | 시간 초과 | `IDLE` | 게임 축 G23, 프로세스 정리 |
| C12 | `RUNNING_QUERY` | 코드 5 | `IDLE` | 게임 축 G16·G17 |
| C13 | `RUNNING_QUERY` | 응답 계약 불일치 | `IDLE` | 게임 축 G13 |
| C14 | `RUNNING_QUERY` | 프로세스 종료·EOF | `IDLE` | 게임 축 G22 |
| C15 | `RUNNING_SEND` | 03 §6.6의 전송 확정(채팅 성공, 행동 없음 또는 성공) | `IDLE`(`COMPLETED`) | 단계 결과 기록. 성공 목록이 채워지기 전에는 도달하지 않음(03 §6.6) |
| C32 | `RUNNING_SEND` | 채팅 성공, 행동 실패 확정 | `IDLE`(`PARTIAL`) | 채팅 재전송 금지, `행동만 다시 보내기` 제공 |
| C33 | `RUNNING_SEND` | 채팅 성공, 행동 결과 불명 | `IDLE`(`PARTIAL`, 행동 불명) | 재전송 버튼 표시 안 함(03 §6.4) |
| C16 | `RUNNING_SEND` | 미전송 확정 | `FAILED` → `IDLE` | 재시도 안내 |
| C17 | `RUNNING_SEND` | 채팅 결과 불명(시간 초과·종료·알 수 없는 응답) | `IDLE`(`UNRESOLVED`) | 자동 재전송 금지, 게임 축 G22 또는 G13 |
| C18 | `RUNNING_ACTION` | 정상 완료 | `RECONCILING` | 작업 후 전체 스냅샷 |
| C19 | `RUNNING_ACTION` | 연결 단절 | `UNKNOWN_OUTCOME` | 자동 재실행 금지, G25 실행, 10분 기산 시작 |
| C20 | `RUNNING_ACTION` | 게임이 실패를 명시 | `FAILED` → `IDLE` | 결과·비용 표시, G25 실행 |
| C21 | `RUNNING_ACTION` | 다회 호출 중 일부만 성공 후 실패 확정 | `IDLE`(`PARTIAL`) | 성공한 호출 수·비용 표시, G25 실행. 마지막 호출이 불명이면 C19처럼 `UNKNOWN_OUTCOME` |
| C22 | `RUNNING_ACTION` | 최대 허용 시간 초과 | `UNKNOWN_OUTCOME` | 프로세스는 임의 종료하지 않음(§8.2), G25 실행, 10분 기산 시작 |
| C23 | `RUNNING_ACTION` | 종료 코드 3 | `UNKNOWN_OUTCOME` | `WAIT_CANCELED` 기록, 사용자 취소로 단정하지 않음, 화해로 실제 실행분 확인, G25 실행 |
| C34 | `RUNNING_ACTION` | 응답 계약 불일치·알 수 없는 `status`·잘못된 JSON | `UNKNOWN_OUTCOME` | 게임 축 G13, G25 실행, 10분 기산 시작 |
| C24 | `RECONCILING` | 화해 조회로 결과 확인 | `IDLE`(`COMPLETED`) | 결과와 비용 표시 |
| C25 | `RECONCILING` | 화해 조회 실패 또는 판정 불가 | `IDLE`(`COMPLETED` + `reconcile_incomplete`) | "완료됐어요. 결과 확인은 아직 못 했어요". 정상 완료를 뒤집지 않음 |
| C26 | `UNKNOWN_OUTCOME` | 화해 조회로 완료 확인 | `IDLE`(`COMPLETED`) | 결과와 비용 표시 |
| C27 | `UNKNOWN_OUTCOME` | 화해 조회로 미실행 확인 | `FAILED` → `IDLE` | 결과 표시 |
| C28 | `UNKNOWN_OUTCOME` | 화해 조회로 일부 실행 확인 | `IDLE`(`PARTIAL`) | 결과와 비용 표시 |
| C29 | `UNKNOWN_OUTCOME` | 화해 조회 3회가 실패 또는 판정 불가(둘 합산), 또는 `UNKNOWN_OUTCOME` 진입 후 10분 경과 `[후보]` | `IDLE`(`UNRESOLVED`) | 재실행 금지 표시. `UNKNOWN_OUTCOME` 진입은 C19·C22·C23·C30·C34 |
| C30 | 시작 시 미완료 컨텍스트 | `ACTION`(앱 비정상 종료 뒤 재시작) | `UNKNOWN_OUTCOME` | 10분 기산 시작 |
| C35 | `UNKNOWN_OUTCOME` | 게임 축이 `READY`가 됨(G20·G26 이후) | 상태 유지 | 화해 조회 시작 |
| C31 | 시작 시 미완료 컨텍스트 | `CHAT`·`BEHAVIOUR` | `IDLE`(`UNRESOLVED`) | 확인 불가 표시 |

**화해 판정 규칙**: 화해는 명령별로 정한 **비교 필드**의 작업 전후 값을 비교한다(예:
수거는 가방 항목, 채집은 획득 품목·재화). 비교 필드와 허용 오차의 명령별 표는 구현
4단계 설계에서 확정한다. **판정 불가**(필드 없음·값이 겹침)와 조회 호출 실패는 종류를 구분해 기록하되 종결 횟수 3회에는 **합산**한다. `RECONCILING`에서 `PARTIAL`이 확인되면 C28을 따른다. 비교 필드 표는 구현 4단계 착수의 전제 조건이다. 절전 중에는 단조 시계가 멈춘 만큼만 10분에서 제외한다.

## 6. 구현 구조와 순서

연결 감지보다 공용 실행 기반을 먼저 만든다.

### 6.1 `CliAdapter`

- 환경 변수 → 기본 경로 → `PATH` 순으로 CLI 탐색
- 프로세스 생성, stdout·stderr 수집, UTF-8 JSON 파싱
- 종료 코드와 실행 시간을 구조화한 원시 결과 반환
- 직접 사용자 메시지·토큰을 로그에 기록하지 않음
- 명령별 의미 판정은 하지 않음

### 6.2 명령별 응답 검증기

| 종류 | 성공 계약 |
|---|---|
| `status` | 객체이며 `pipe`가 알려진 값 |
| `capabilities` | 객체이며 `commands` 배열 또는 최상위 `loading=true` |
| 일반 조회 | 해당 명령의 객체·배열 스키마 |
| 실행 명령 | 명령별 `status`, `body`, `error`, `result` 계약 |

알 수 없는 구조는 성공으로 추측하지 않고 `PROTOCOL_ERROR`로 보존한다.

### 6.3 최소 `CommandBroker`

- 앱 내부의 모든 CLI 호출 직렬화
- 요청 ID·세대 번호·명령 분류 부여
- 조회 제한 시간과 취소 토큰 관리. 채팅 전송에는 별도 제한 시간(03 §6.6)
- 사용자 명령과 주기 조회 우선순위 관리
- 같은 종류의 주기 조회는 최신 하나만 유지
- 앱 단일 실행 잠금과 명령 잠금을 분리

현재 `mabinogi_chat.py`의 직접 프로세스 호출은 구현 단계에서 이 어댑터로 옮긴다.
외부 프로그램의 직접 CLI 호출까지 차단할 수 없다는 한계는 진단 화면에 명시한다.

### 6.4 `ConnectionSupervisor`

공용 중개기 위에서 `status`, 스키마, 캐릭터 세션을 확인하고 상태 전이를 만든다.

### 6.5 `SnapshotScheduler`와 `EventDetector`

연결 준비 이후에만 시작하며 오래된 값으로 이벤트를 만들지 않는다.

## 7. 대기열 정책

우선순위:

1. 사용자가 직접 요청한 명령
2. 결과 불명 복구와 연결 확인
3. 작업 완료 후 화해 조회
4. 정기 조회

규칙:

- 정기 조회는 동일 종류당 최신 하나만 유지한다.
- `RUNNING_ACTION` 중 놓친 정기 조회는 폐기한다.
- 작업 종료 후 전체 스냅샷 한 번으로 대체한다.
- 사용자 명령의 최대 대기 건수는 1차 구현에서 1건(`PendingSlot`)이며 승인된 `ACTION`·AI 조회를 센다. `@@` 초안은 포함하지 않는다. 이미 차 있으면 두 번째 요청은 거부한다(C5).
- 대기 중 도착한 승인은 실행 직전에 재검증하고 유효 시간이 지났으면 실행하지 않는다(03 §5.2).
- 장시간 `ACTION` 중 AI가 시작한 `QUERY`는 대기시키거나, 최근 `fresh` 스냅샷이 있으면 그 값과 관찰 시각을 보여준다. 다른 CLI 호출은 보내지 않는다.
- `@@` 채팅도 장시간 작업 중 즉시 실행하지 않고 대기 상태와 이유를 표시한다.
- 대기 중인 `@@` 문구는 사용자가 취소·수정할 수 있으며 조건이 해소돼도 자동 발송하지 않고 사용자가 `보내기`를 눌러야 나간다. 세부 규칙은 `03-명령-실행과-안전-설계.md` 6.5.

## 8. 제한 시간·취소·앱 종료

### 8.1 짧은 조회

- 초기 제한 시간 후보는 5초다.
- 제한 시간은 실제 측정 후 확정한다.
- 시간 초과 시 프로세스를 정리하고 `SLOW_RESPONSE`로 분류한다.
- 한 번의 시간 초과로 게임 단절을 확정하지 않는다.

### 8.2 장시간 실행

- 채집·제작처럼 완료까지 차단되는 명령은 짧은 제한 시간을 적용하지 않는다.
- 게임이 자체적으로 종료 사유를 응답하도록 최대 도구 허용 시간까지 기다린다.
- 앱은 임의로 프로세스를 먼저 종료하지 않는다.
- 프로세스 생존, EOF, 비정상 종료는 별도 감시한다.
- 동시 `status` 호출의 안전성이 확인되기 전에는 다른 CLI 호출을 보내지 않는다.
- 앱 내 취소 버튼은 동시 호출 안전성 검증 전에는 제공하지 않고 게임 안에서
  중지하도록 안내한다.

### 8.3 앱 종료

- 실행 중 앱 종료 요청에는 작업 진행 중임을 알린다.
- 기본값은 앱을 숨기고 작업 완료를 기다리는 것이다.
- 강제 종료된 경우 다음 시작에서 `UNKNOWN_OUTCOME` 복구 흐름으로 진입한다.
- 상태 변경 명령을 자동 재실행하지 않는다.

종료 코드 3은 사용자 취소로 단정하지 않고 `WAIT_CANCELED`로 기록한다. 사용자
취소 여부는 응답 본문과 최근 명령 기록으로 확인된 경우에만 표시한다.

## 9. 재시도와 화해 정책

| 명령 종류 | 자동 재시도 |
|---|---|
| 읽기 전용 조회 | 일시 오류에서 제한적으로 허용 |
| `capabilities` | 코드 4에서 갱신 후 한 번 |
| 채팅·행동 | 자동 재시도 금지. 미전송 확정과 불명의 구분은 03 §6.6 |
| 채집·제작·변형·수거 | 자동 재시도 금지 |

채팅+행동은 하나의 논리 작업으로 묶되 `chat_sent`, `behaviour_sent`를 따로
기록한다. 행동이 실패해도 이미 전송된 채팅을 다시 보내지 않는다. rate limit은
`retryAfterSeconds`를 사용자에게 보여주고 자동 재시도 여부를 별도 정책으로 둔다.

작업 컨텍스트, 결과 분류, 승인 모델은 `03-명령-실행과-안전-설계.md`가 정한다.

`requiresConfirm`은 문자열과 불리언을 모두 정규화한다. 알 수 없는 값과 누락은
안전 우선으로 확인 필요로 처리한다. `@@` 직접 입력은 사용자의 명시적 채팅 전송
요청이라는 예외 정책을 적용한다.

## 10. 연결 확인과 정보 수집 주기

### 10.1 연결 확인 후보값

| 상황 | 후보 주기 |
|---|---|
| 앱 시작 | 즉시 |
| 연결 전·재연결 | 2초 → 5초 → 10초 → 최대 30초 + 작은 무작위 지연 |
| 정상 연결 | 10초 |
| 앱 백그라운드 | 30초 |
| 장시간 작업 | 다른 CLI 호출 중지, 프로세스 생존만 감시 |

명확한 코드 5는 즉시 원인별 상태로 전환한다. 단순 시간 초과는 3회 또는 15초
동안 지속될 때 연결 불안정을 확정하는 후보 규칙으로 두고 실측 후 승인한다.

### 10.2 정보 수집 후보값

| 정보 | 후보 주기 | 사용 조건 |
|---|---|---|
| 활동 상태 | 15초 | `READY`, 명령 유휴 |
| 체력·만복도·가방 | 30초 | `READY`, 명령 유휴 |
| 위치·날씨·시간 | 1분 | 상황형 제안 사용 시 |
| 일일·주간 미션 | 5분 또는 요청 시 | 미션 제안 사용 시 |
| 재화 | 5분, 비용 작업 전후 | 비용 작업 사용 시 |
| 연금 대기열 | 로컬 ETA 근처 | 등록된 작업이 있을 때 |
| 주변 플레이어 | 요청 시 | 기본 상시 조회 금지 |

## 11. 스냅샷 계약

각 데이터 영역은 다음 메타데이터를 가진다.

- `observed_at`: 실제 조회 시각
- `source_command`: 원본 명령
- `valid_until`: 이벤트 판단 유효 시각
- `quality`: `fresh`, `stale`, `partial`, `invalid`
- `request_id`: 원본 요청
- `related_action_id`: 작업 전후 비교용 ID (`03`의 `ActionContext.action_id`)
- `last_ok_at`: 해당 영역의 마지막 정상 응답 시각

부분 실패 시 마지막 정상값은 “오래된 값”으로 표시할 수 있지만 제안 판단에는
사용하지 않는다. 재연결 직후에는 기준 스냅샷을 새로 만들되, 이전 작업의 결과
화해가 끝나기 전까지 변화 이벤트를 발생시키지 않는다.

## 12. 첫 실행과 복구 사용자 흐름

첫 실행 단계:

1. 게임 찾기
2. 게임 실행 확인
3. 게임 설정의 연결 기능(MM AI 에이전트) 확인
4. 캐릭터 접속 확인
5. 준비 완료와 첫 브리핑

AI 설정은 위 단계와 **독립된 선택 단계**다. 미설정이면 `AI 없이 사용`으로 시작하며 제공
범위는 03 §7.1이 정한다. 어느 단계에서든 설정으로 들어가 `AI 연결하기`를 할 수 있다.

상태별 공통 행동:

- `다시 확인`
- `설정 위치 보기`
- `경로 직접 지정`
- `진단 정보 복사`

연결 실패 중에도 입력 중인 AI 대화와 전송하지 않은 `@@` 문구를 보존한다. 복구
메시지는 “연결됨”만 알리지 않고 중단된 기능, 명령 결과 확실성, 다음 행동을 함께
보여준다. 첫 브리핑은 하루 첫 실행 또는 중요한 변화 때 한 번만 보여주며, 사용자가
먼저 입력하면 대화를 가로막지 않는다.

첫 선택지:

- `오늘 할 일 추천`
- `내 상태 쉽게 설명`
- `@@ 게임 채팅 사용법`

## 13. 선제 제안과 알림 피로 방지

제안 등급:

- `긴급`: 연결 복구 필요, 결과 불명, 즉시 위험 상태
- `도움`: 가방·만복도·미션 등 행동 제안
- `참고`: 날씨·주변 상황·기록 정보

규칙:

- 한 화면에 가장 중요한 제안 하나만 우선 표시한다.
- 가방은 90% 진입 때 알리고 80% 아래로 복귀한 뒤 다시 90%를 넘을 때 재알림한다.
- 같은 이벤트에는 유예 시간을 적용한다.
- `1시간 조용히`, `오늘은 숨기기`, `이 제안 끄기`를 제공한다.
- 최초 스냅샷이라도 체력 위험·가방 99% 같은 즉시 위험 상태는 한 번 알린다.
- 연결 흔들림은 새 메시지를 쌓지 않고 하나의 상태 카드만 갱신한다.

정확한 임계값과 유예 시간은 UI 목업 검토와 실제 사용 테스트 뒤 확정한다.

## 14. 접근성 요구

- 상태를 색상만으로 구분하지 않고 텍스트·아이콘·형태를 함께 사용한다.
- 모든 기능을 키보드만으로 실행할 수 있어야 한다.
- 새 제안이 입력창 초점을 빼앗지 않는다.
- 화면 읽기 프로그램에는 중요한 상태 변경만 알린다.
- Windows 고대비, 125~200% 배율, 큰 글자, 동작 줄이기를 지원한다.
- 터치 대상은 충분한 크기를 유지한다.

## 15. 테스트 형식과 합격 기준

모든 테스트는 다음 필드를 가진다. 시나리오는 하나의 통합 목록으로 관리한다: **02 §5의 전이 행 G1~G34, A1~A12, C1~C31**(행마다 모의 CLI 테스트 1건 이상)과 **03 §7의 R1~R8과 03 §9의 T1~T50**이다. 합격 기준(사용성·접근성 수치 포함)은 03 §9.1이 정한다.

| 사전 상태 | 입력·사건 | 기대 내부 상태 | 사용자 표시 | 사용 가능 기능 | 재시도 규칙 |
|---|---|---|---|---|---|

필수 시나리오:

- CLI 없음·권한 거부·공백/한글 경로·버전 불일치
- 게임 꺼짐·옵션 꺼짐·로그인 전·캐릭터 선택·정상 접속
- 무응답·느린 응답·비정상 종료·잘못된 JSON·과도한 출력
- 코드 2·3·4·5와 명령별 오류 응답
- 연결 상태가 빠르게 반복 변경됨
- 장시간 작업 중 게임 종료·앱 종료·외부 CLI 호출
- 결과 불명 작업의 재시작 후 화해
- 절전 복귀와 시스템 시간·시간대 변경
- 사용자가 입력 중일 때 선제 제안 도착
- 알림 숨김·유예 설정의 재시작 후 유지
- 200% 배율·고대비·키보드·화면 읽기
- 승인 카드 열림 중 게임 끊김, 대기 승인의 만료, 게임 정보 신선도
- `SLOW_RESPONSE` 출구, AI 축 전이, 미사용 상태의 도달 불가 확인

단계 통과 기준:

- 통합 목록(G·A·C·T) 자동 테스트 전부 통과
- 높은 중요도 미해결 문제 0건
- 예상하지 못한 응답에서 자동 상태 변경 명령 0건
- 실제 게임 검증 전 모의 CLI 테스트 통과
- 서브에이전트 재검토 완료
- 사용자 승인 기록

## 16. 구현과 승인 단계

| 단계 | 산출물 | 승인 관문 |
|---|---|---|
| 1. 설계 수정 | 이 문서, 03, 독립 리뷰 결과 | 재검토 승인 |
| 2. CLI 기반 | `CliAdapter`, 명령별 검증기, 최소 `CommandBroker`, 모의 CLI, `ActionContext`·결과 분류·응답 판정표(03 §3·§4·§6.6), 기존 `mabinogi_chat.py` 결함 수정(03 §10) | 리뷰·승인 |
| 3. 연결 감지 | `ConnectionSupervisor`, 세 축 상태 전이, 복구 UI 데이터 | 리뷰·승인 |
| 4. 상태 수집 | 스냅샷·스케줄·대기열 합치기, `ACTION` 화해(전후 스냅샷 비교) | 리뷰·승인 |
| 5. 선제 제안 | 우선순위·유예·알림 설정 | 리뷰·승인 |
| 6. AI 대화 | 공급자 인터페이스·로컬 대체 동작 | 리뷰·승인 |
| 7. Windows 앱 | 채팅 UI·접근성·배포 | 최종 승인 |

### 16.1 시나리오의 단계별 적용

| 단계 | 통과해야 하는 시나리오 |
|---|---|
| 2. CLI 기반 | C1~C3, C10~C17, C32~C34(명령 축 중 `QUERY`·`CHAT`), 03의 T1~T13, T17~T20, T31~T36, T40, 채팅 응답 판정 |
| 3. 연결 감지 | G1~G35, A1~A12, C11~C14 |
| 4. 상태 수집 | C4~C9, C18~C31, C35, T11~T13, T21~T23, T37~T39, 스냅샷·화해 |
| 5. 선제 제안 | 알림·유예·재시작 후 설정 유지(신규 ID를 이 단계 착수 전에 부여) |
| 6. AI 대화 | A13·R1~R8, T14~T16, T24~T26, T30 |
| 7. Windows 앱 | 사용성 시험, 접근성 수치, 200%·고대비·키보드·화면 읽기, 앱 종료·숨김, 단일 실행 잠금 |

02 §15의 필수 시나리오 중 ID가 아직 없는 항목(공백·한글 경로, 버전 불일치, 과도한 출력, 종료 코드 2·4, 시간 변경 때의 카드 만료)은 해당 단계 착수 전에 ID를 부여한다.

## 17. 리뷰 기록

| 날짜 | 상태 | 내용 |
|---|---|---|
| 2026-09-21 | 검토 대기 | 최초 계획서 작성 |
| 2026-09-21 | 변경 필요 | 서브에이전트 3인 독립 리뷰, 승인 보류 |
| 2026-09-21 | 재검토 대기 | 독립 상태 축, 전이표, 중개기 우선, 장시간 작업, 응답 검증, 복구 UX, 테스트 기준 반영 |
| 2026-09-21 | 변경 필요 | 02·03 통합 리뷰 3인, 승인 보류 (`reviews/02-03-기능설계-리뷰-2.md`) |
| 2026-09-21 | 재검토 대기 | 세 축 전체 전이표, `RUNNING_SEND`, 용어 대응, 가정 표시, 03 연동 반영 |
| 2026-09-21 | 변경 필요 | 재리뷰 3인: 안전 승인, 아키텍처·UX 승인 보류 |
| 2026-09-21 | 재검토 대기 | 전이표 행 ID화·초기 단계 출구·`PendingSlot`·`SESSION_CHECKING`·화해 규칙 반영 |
| 2026-09-21 | 변경 필요 | 3차 리뷰: UX 승인, 아키텍처 승인 보류(높음 1건) |
| 2026-09-21 | 재검토 대기 | `probe_paused_by_action` 해제 확대, 세션 상실·`ACTION` 계약 불일치·코드 3·슬롯·화해 트리거 행, 단계별 시나리오 표 반영 |

## 18. 남은 검증 항목

1. `status`와 장시간 작업 동시 호출 안전성
2. 캐릭터 미접속 상태의 실제 오류 응답
3. 이모지 포함 채팅 길이 계산 단위
4. 짧은 조회의 적절한 제한 시간
5. 기본 알림 임계값과 유예 시간
6. 코드 5 응답 본문으로 `game_off`·`option_off`를 구분할 수 있는지
7. 종료 코드 2·3·4의 실제 의미와 코드 3의 사용자 취소 여부
8. `retryAfterSeconds` 필드와 rate limit 응답 형식
9. `SLOW_RESPONSE` 확정 규칙(3회 또는 15초)과 백오프 값의 실측
10. `loading` 지속 5분 상한, 연결 확인·조회 주기, 조회 제한 시간의 실측

03 §12에 명령·응답 관련 검증 항목이 더 있다.

실제 게임 상태를 변경하는 검증은 별도 승인 후 수행한다.
