# 마비노기모바일 AI도우미 — 명령 실행과 안전 설계

| 항목 | 내용 |
|---|---|
| 문서 상태 | 검토 대기 |
| 작성일 | 2026-09-21 |
| 선행 문서 | `02-연결-상태-설계.md` |
| 후속 문서 | `04-UI-상세설계.md` |
| 다음 관문 | 02·03 통합 서브에이전트 리뷰 |

## 1. 목적과 범위

02가 "연결과 상태"를 다룬다면 이 문서는 "무엇을 어떤 조건으로 실행하고, 결과를
어떻게 분류하며, 사용자가 무엇을 승인하는가"를 정한다. UI 문서(04)가 표시할 모든
값(작업 이름, 결과 분류, 승인 화면 내용, 대기 문구)은 이 문서와 02에서만 나온다.

포함: 명령 분류, 실행 컨텍스트, 결과 분류, 승인 모델, `@@` 채팅 파이프라인, 대기
정책, AI 대화 요청 상태, 저장·개인정보 원칙.

제외: AI 공급자 구현·과금·로그인, 실제 GUI 코드, 자동 실행형 시나리오.

## 2. 명령 분류

| 분류 | 예 | 자동 재시도 | 승인 |
|---|---|---|---|
| `QUERY` 읽기 조회 | 상태·가방·활동 조회 | 제한적 허용 | 불필요 |
| `CHAT` 게임 채팅 | 채팅 전송 | 금지 | `@@` 직접 입력은 예외, 그 외 필수 |
| `BEHAVIOUR` 행동 | 손인사·하트 등 | 금지 | 채팅에 종속되거나 별도 승인 |
| `ACTION` 상태 변경 | 채집·제작·변형·수거 | 금지 | 필수 |

분류는 `capabilities`의 메타데이터에서 도출한다. 분류할 수 없거나 알 수 없는
명령은 `ACTION`으로 취급해 승인을 요구하고, AI가 계획한 명령이 `capabilities`에
없으면 실행하지 않고 거부한다. 명령 인자는 실행 전 스키마로 검증한다.

## 3. 실행 컨텍스트 `ActionContext`

모든 `CHAT`·`BEHAVIOUR`·`ACTION` 실행은 다음 값을 가진 컨텍스트로 추적한다.
02의 스냅샷 `related_action_id`가 이 컨텍스트를 가리킨다.

| 필드 | 설명 |
|---|---|
| `action_id` | 작업 고유 ID |
| `label` | 사용자에게 보일 작업 이름(중개기가 명령에서 생성, AI 문장 아님) |
| `kind` | 2장의 분류 |
| `origin` | `USER_AT`(직접 입력 `@@`), `AI_APPROVED`(승인 카드로 승인), `SYSTEM` |
| `command`, `args` | 실제 실행 명령과 검증된 인자 |
| `cost` | `capabilities` 메타데이터의 비용. 알 수 없으면 `unknown` |
| `created_at`, `started_at`, `ended_at` | 시각 |
| `steps` | 논리 작업의 단계(`chat`, `behaviour` 등)와 단계별 `sent` 결과 |
| `outcome` | 4장의 결과 분류 |

컨텍스트의 미완료 항목(`started_at` 있고 `ended_at` 없음)은 로컬에 보존해 앱이
비정상 종료된 뒤 다음 시작에서 `UNKNOWN_OUTCOME` 복구(02 §8.3)에 사용한다. 보존
내용에 사용자 채팅 문구는 저장하지 않는다(8장).

## 4. 결과 분류 `Outcome`

| 결과 | 의미 | 02 `CommandState` 연결 |
|---|---|---|
| `COMPLETED` | 완료 응답 확인, 화해 완료 | `RECONCILING` → `IDLE` |
| `PARTIAL` | 논리 작업의 일부 단계만 성공(예: 채팅 성공·행동 실패) | `IDLE`(재시도 금지 표시) |
| `FAILED` | 실패 확정 | `FAILED` → `IDLE` |
| `UNVERIFIED` | 실행 여부를 확정할 수 없음, 화해 진행 중 | `UNKNOWN_OUTCOME` |
| `UNRESOLVED` | 화해를 시도했으나 끝내 확정 불가 | `UNKNOWN_OUTCOME` → `IDLE` |

- `RECONCILING`은 정상 완료 뒤 확인 단계이고 성공을 뒤집지 않는다. 표시에서
  `UNVERIFIED`와 구분한다.
- 게임 상태로 결과를 비교할 수 있는 `ACTION`은 작업 전후 스냅샷으로 화해한다.
  비교할 수 없는 `CHAT`·`BEHAVIOUR`는 화해하지 않는다. 결과 불명이면 곧바로
  `UNRESOLVED`로 종결하고 "전송 여부를 확인할 수 없어요, 다시 보내지 않았어요"로
  표시한다. 재전송은 사용자의 새 입력으로만 가능하다.
- 02 §5에 `UNKNOWN_OUTCOME` → `IDLE`(결과: `UNRESOLVED`) 전이를 추가한다.

## 5. 승인 모델

### 5.1 승인이 필요한 경우

`CHAT`(`@@` 직접 입력 예외 제외), `BEHAVIOUR`(단독 실행 시), `ACTION`은 모두
사용자의 명시적 승인이 필요하다. `requiresConfirm`이 누락되거나 알 수 없는 값이면
확인 필요로 처리한다(02 §9).

### 5.2 AI 경로의 승인 카드

AI가 제안한 실행은 선택지 클릭만으로 실행되지 않는다. 클릭하면 승인 카드가
열리고, 카드의 내용은 AI 문장이 아니라 검증된 `ActionContext`(`label`, `command`,
`args`, `cost`)로만 만든다.

| 카드 항목 | 출처 |
|---|---|
| 하는 일 | 중개기가 명령에서 생성한 `label` |
| 보낼 채널·내용 | 검증된 `args` |
| 비용 | `cost`, 알 수 없으면 "비용 확인 불가" |
| 선택지 | `실행`, `취소` — 두 선택지의 기본 초점은 `취소` |

- 카드의 `실행`이 승인이다. 승인은 해당 `action_id` 1건에만 유효하다.
- 승인 유효 시간 후보값은 2분이다. 지나면 카드를 닫고 다시 요청받는다.
- AI 문장이 카드 내용과 다르면 카드가 우선한다.

### 5.3 `@@` 예외의 범위

`@@` 예외는 **사용자가 입력창에서 직접 확정한 문구**에만 적용한다. AI 응답 문자열,
선택지 텍스트, 자동 채워진 값은 예외 대상이 아니다. 예외에는 다음이 포함된다.

- 사용자가 입력한 문구와 8장의 규칙으로 정규화한 문구
- 자동으로 붙는 이모지 1개
- 자동 행동은 예외에 포함하지 않는다. 6.3의 미리보기에 함께 보이고, 사용자가
  전송 버튼이나 Enter를 누르는 것으로 전체 내용에 동의한 것으로 본다. 설정에서
  자동 행동을 끌 수 있다.

## 6. `@@` 채팅 파이프라인

### 6.1 입력 판정

화면의 모드 표시와 실제 전송은 **같은 함수** `parse_chat_input`을 사용한다. UI가
판정을 재구현하지 않는다.

- 앞 공백을 제거한 뒤 `@@`로 시작하면 게임 채팅이다.
- `@@`뒤 공백 여부는 동일하게 취급하고 본문이 비면 전송하지 않는다.
- 본문은 다시 공백을 제거한다. `/`나 `#`로 시작하는 본문은 명령어 주입을 막기 위해
  거부하고 이유를 알린다.
- 전각 `＠＠`는 게임 채팅으로 인식하지 않는다.

### 6.2 길이

게임 채팅 한도는 50자다. 자동으로 붙는 이모지 접미(공백 1 + 이모지 1 = 2자)를
제외해 사용자 입력은 최대 **48자**다. 계산 단위는 현재 코드포인트이며 게임의 실제
계산 단위는 미검증 항목이다(02 §18). 검증되기 전에는 코드포인트 기준 값을 표시하고
초과 전송이 게임에서 거부되면 그 응답을 사용자에게 알린다.

### 6.3 전송 전 미리보기

입력창 아래에 최종 전송 내용을 한 줄로 보여준다: `보낼 말: 고마워요 😍 (행동: /하트)`.
자동 행동이 없으면 행동 부분을 생략한다. 사용자는 설정에서 자동 행동을 끌 수 있다.

### 6.4 단계 전송과 부분 성공

채팅과 행동은 하나의 논리 작업이며 `steps`에 `chat_sent`, `behaviour_sent`를
따로 기록한다.

| 채팅 | 행동 | 결과 | 사용자 표시 | 재시도 |
|---|---|---|---|---|
| 성공 | 성공 | `COMPLETED` | 보냈어요 | — |
| 성공 | 실패 | `PARTIAL` | 채팅은 보냈고 행동만 실패했어요. 채팅은 다시 보내지 않아요 | 행동만 사용자가 다시 요청 |
| 실패 | 미실행 | `FAILED` | 채팅을 보내지 못했어요 | 사용자가 다시 보냄 |
| 무응답·결과 불명 | 미실행 | `UNRESOLVED` | 전송됐는지 확인할 수 없어요 | 자동 재전송 금지 |

### 6.5 대기 정책

`ACTION` 실행 중, 게임 연결 문제 중, 결과 불명 중에 입력한 `@@`는 대기 슬롯 1개에
보존한다.

- 대기 문구는 **자동으로 발송하지 않는다.** 조건이 해소되면 "지금 보낼까요?"를
  알리고 사용자가 `보내기`를 눌러야 나간다. 화면에 입력한 시각을 함께 보여준다.
- 대기 중 새 `@@`를 입력하면 기존 문구를 교체할지 묻는다.
- 대기 문구는 수정·삭제할 수 있고, 앱 종료 시 저장하지 않는다.
- 게임 끊김 중에는 채팅 입력을 막지 않고 초안으로 보존하며 `보내기`만 비활성화하고
  이유를 보인다(02 §12).

## 7. AI 대화 요청 상태

`AIConnectionState`(02 §4.2)는 연결 자체를 나타내고, 각 대화 요청은 별도 상태를
가진다.

| 상태 | 의미 | 사용자 행동 |
|---|---|---|
| `AI_IDLE` | 진행 중 요청 없음 | 입력 |
| `AI_THINKING` | 응답 대기 | `중단` |
| `AI_STREAMING` | 응답 수신 중 | `중단` |
| `AI_TOOL_WAITING` | 게임 조회 결과 대기 | `중단` |
| `AI_FAILED` | 실패 확정 | `다시 묻기` |
| `AI_CANCELED` | 사용자가 중단 | 입력 |

- 첫 응답 제한 시간 후보값은 30초, 수신 중 무진행 후보값은 15초다. 초과하면
  사용자에게 알리고 `AI_FAILED`로 종결한다.
- 요청이 실패하면 대화에 실패 응답을 남긴다("답을 받지 못했어요. 다시 물어볼까요?").
  연결 오류가 사용자 질문을 무응답으로 남기지 않는다.
- 게임 조회가 `SLOW_RESPONSE`나 실패이면 조회 실패 사실을 AI에게 전달하고, AI가
  없으면 로컬 문구로 답한다.
- `AUTH_ERROR`와 `NOT_CONFIGURED`는 자동 재시도하지 않고 설정을 안내한다.
  `RATE_LIMITED`는 다시 쓸 수 있는 시각을 표시한다.
- AI 응답에서 명령을 추출할 때는 2장의 검증과 5장의 승인을 거친다.

### 7.1 첫 실행의 AI 설정

AI 설정은 게임 연결 검사(02 §12)와 독립된 선택 단계다. 미설정이면 `AI 없이 사용`으로
바로 시작할 수 있다. 이 경우 첫 브리핑은 로컬 규칙 기반 문구를 사용한다. 설정은
언제든 설정 화면에서 할 수 있으며, 공급자 종류와 키 관리는 별도 설계로 미룬다.

## 8. 저장과 개인정보

- 사용자 채팅 문구와 AI 대화 내용은 로그·진단 정보에 기록하지 않는다.
- 대화 기록의 기본값은 현재 실행 중에만 보관하고 종료 시 폐기한다. 저장은 사용자가
  켰을 때만 하며, 저장 범위와 삭제 방법을 설정에서 보여준다.
- 진단 정보 복사 전에 복사될 내용을 미리 보여준다. 개인정보·채팅 문구·자격증명은
  제거한다. 토큰 전체 값은 어디에도 기록하지 않는다.
- 진단 화면에 "이 앱 밖에서 실행하는 CLI 호출은 막을 수 없어요" 한계를 명시한다.
- 실행 컨텍스트 보존 파일에는 명령 이름·시각·결과만 남기고 채팅 문구는 남기지 않는다.

## 9. 테스트 시나리오(추가분)

02 §15의 형식(사전 상태 / 입력·사건 / 기대 내부 상태 / 사용자 표시 / 사용 가능
기능 / 재시도 규칙)을 따른다.

- `@@` 판정: 앞 공백, `＠＠`, 본문 없음, 48자 경계, `/`·`#` 시작
- 채팅 성공·행동 실패의 부분 성공, 채팅 무응답의 `UNRESOLVED`
- 장시간 작업 중 `@@` 대기, 재연결 뒤 자동 발송 없음, 대기 교체
- AI가 제안한 `ACTION`: 카드 내용 = 검증된 인자, 승인 유효 시간 만료
- AI 응답 문자열에 `@@` 문구가 있어도 예외가 적용되지 않음
- `capabilities`에 없는 명령의 거부, 알 수 없는 `requiresConfirm`
- AI 요청 무응답·중단·스트리밍 끊김·게임 조회 실패 시 대화 응답
- 앱 강제 종료 후 재시작 시 미완료 컨텍스트의 `UNVERIFIED` 진입
- 실제 게임 상태를 바꾸는 검증은 별도 사용자 승인 뒤 수행

## 10. 구현 시 반영할 기존 코드 문제

현재 `mabinogi_chat.py`에 다음 문제가 있다. 구현 2단계(`CliAdapter`)에서 함께 고친다.

- 채팅 전송 뒤 행동 전송이 실패하면 예외 하나로 합쳐져 "채팅은 이미 나갔다"는
  정보가 사라진다. 6.4의 단계 결과를 반환하도록 바꾼다.
- 응답이 JSON 배열이면 일반 예외로 빠진다. `PROTOCOL_ERROR`로 분류한다.
- 입력 판정 함수는 UI와 공유한다.

## 11. 02와의 관계

이 문서가 요구하는 02의 변경은 02 §5(`UNKNOWN_OUTCOME` → `IDLE` 종결 전이), §7
(대기 정책 참조), §9(`ActionContext`·승인 참조), §11(`related_action_id` 정의)이다.
02가 게임·AI·명령 세 축을 다루고, 이 문서가 명령 축의 내부 의미와 안전 규칙을 다룬다.

## 12. 리뷰 기록

| 날짜 | 상태 | 내용 |
|---|---|---|
| 2026-09-21 | 검토 대기 | 02 리뷰와 04 UI 리뷰에서 확인된 기능 설계 공백을 채워 작성 |
