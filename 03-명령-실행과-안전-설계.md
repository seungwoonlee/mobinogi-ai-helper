# 마비노기모바일 AI도우미 — 명령 실행과 안전 설계

| 항목 | 내용 |
|---|---|
| 문서 상태 | 재검토 대기 |
| 작성일 | 2026-09-21 |
| 수정일 | 2026-09-21 |
| 선행 문서 | `02-연결-상태-설계.md` |
| 후속 문서 | `04-UI-상세설계.md` |
| 관련 리뷰 | `reviews/02-03-기능설계-리뷰-2.md` |
| 다음 관문 | 수정본 서브에이전트 재검토 |

## 1. 목적과 범위

02가 "연결과 상태"를 다룬다면 이 문서는 "무엇을 어떤 조건으로 실행하고, 결과를
어떻게 분류하며, 사용자가 무엇을 승인하는가"를 정한다. UI 문서(04)가 표시할 작업
이름, 결과 분류, 승인 화면 내용, 대기 문구는 이 문서와 02에서 공급한다.

포함: 명령 분류, 실행 컨텍스트, 결과 분류, 응답 판정, 승인 모델, `@@` 채팅
파이프라인, 대기 정책, AI 대화 요청 상태, AI 없이 사용하는 범위, 저장·개인정보.

제외: AI 공급자 구현·과금·로그인, 실제 GUI 코드, 자동 실행형 시나리오.

**표기 규칙**: 실제 응답으로 확인하지 못한 값은 `[가정]`, 실측 전 임시로 정한 수치는
`[후보]`로 표시한다. 두 표시는 02 §18 또는 이 문서 §12의 검증 목록에 항목이 있어야
한다.

**현재 동작과 목표 설계**: 현재 `mabinogi_chat.py`는 `@@` 줄마다 미리보기 없이 즉시
전송한다(README §7의 설명). 이 문서는 GUI 앱에서 달성할 목표 설계를 정한다.

## 2. 명령 분류

| 분류 | 예 | 자동 재시도 | 승인 |
|---|---|---|---|
| `QUERY` 읽기 조회 | 상태·가방·활동 조회 | 제한적 허용 | 불필요 |
| `CHAT` 게임 채팅 | 채팅 전송 | 금지 | `@@` 직접 입력 예외, 그 외 필수 |
| `BEHAVIOUR` 행동 | 손인사·하트 등 | 금지 | 채팅에 종속되거나 별도 승인 |
| `ACTION` 상태 변경 | 채집·제작·변형·수거 | 금지 | 필수 |

- README §4의 "채팅만 확인 필요"는 게임 자체의 `requiresConfirm` 설명이다. 이 앱은
  더 엄격해서 `ACTION`도 승인을 요구한다. 사용자 안내 문서에서 이 차이를 명시한다.
- `BEHAVIOUR`는 현재 코드에서 `write_chat`에 `/손인사1` 같은 문자열로 전송된다
  `[가정: 별도 명령이 아님]`. 그래서 `capabilities` 메타데이터만으로는 `CHAT`과
  구분할 수 없다. 구분은 **허용 행동 목록**(게임의 소셜 행동 조회 명령이 반환한
  이름) 안에 있는 `/행동` 문자열인지로 판정한다. 목록 조회가 안 되면 앱 내장 목록을
  쓰고, 목록에 없는 `/…` 문자열은 거부한다.
- `capabilities`에 분류·비용 메타데이터가 실제로 있는지 확인되지 않았다
  `[가정]`. 없으면 앱 내장 명령 표로 분류하고 비용은 `unknown`으로 표시한다.
- 분류할 수 없거나 알 수 없는 명령은 `ACTION`으로 취급해 승인을 요구한다. AI가 계획한
  명령이 알려진 목록에 없으면 실행하지 않고 거부한다.

### 2.1 명령 상태 경로

| 분류 | 실행 중 `CommandState` | 완료 뒤 | 화해 |
|---|---|---|---|
| `QUERY` | `RUNNING_QUERY` | `IDLE` | 없음 |
| `CHAT`·`BEHAVIOUR` | `RUNNING_SEND` | `IDLE` | 없음(비교 불가). 결과 불명은 곧바로 `UNRESOLVED` |
| `ACTION` | `RUNNING_ACTION` | `RECONCILING` → `IDLE` | 작업 전후 스냅샷 |

`RUNNING_SEND`는 짧은 전송 전용이며 정기 조회 폐기와 전체 스냅샷을 일으키지 않는다.
다만 다른 CLI 호출과 직렬화한다(02 §6.3).

## 3. 실행 컨텍스트 `ActionContext`

모든 `CHAT`·`BEHAVIOUR`·`ACTION` 실행은 다음 값을 가진 컨텍스트로 추적한다. 02의
스냅샷 `related_action_id`가 `action_id`를 가리킨다. 다회 호출이 필요한 작업(채집
500개 등)도 컨텍스트 하나이며 `steps`로 호출을 나눈다.

| 필드 | 설명 | 저장 허용 |
|---|---|---|
| `action_id` | 작업 고유 ID | 예 |
| `label` | 사용자에게 보일 작업 이름(중개기가 명령에서 생성, AI 문장 아님) | 예 |
| `kind` | 2장의 분류 | 예 |
| `origin` | `USER_AT`, `AI_APPROVED`, `SYSTEM` (5.4) | 예 |
| `command` | 명령 이름 | 예 |
| `args` | 검증된 인자 | `ACTION`의 비개인 인자(레시피·횟수)만. `CHAT` 문구는 **저장하지 않음** |
| `cost` | 예상 비용, 총합 계산. 모르면 `unknown` | 예 |
| `created_at`, `started_at`, `ended_at` | 시각 | 예 |
| `steps` | 단계 목록과 단계별 `pending`/`sent`/`failed`/`unknown` | 예 |
| `outcome` | 4장의 결과 분류 | 예 |
| `last_observed_at` | 화해에 쓴 마지막 정상 조회 시각 | 예 |

**미리 기록(write-ahead)**: 각 단계는 전송 **직전**에 `pending`으로, 직후에 결과로
기록한다. 앱이 채팅과 행동 사이에 죽어도 채팅이 나갔을 가능성을 알 수 있다. 저장
파일에는 채팅 문구를 남기지 않으며, 재시작 뒤 화면에는 작업 종류와 시각만 보인다.

## 4. 결과 분류 `Outcome`

| 결과 | 의미 | 02 상태 연결 |
|---|---|---|
| `COMPLETED` | 전송·완료 응답 확인(`ACTION`은 화해까지 끝남) | `IDLE` |
| `PARTIAL` | 논리 작업의 일부 단계만 성공(채팅 성공·행동 실패, 다회 호출 중 일부 성공) | `IDLE` |
| `FAILED` | **미전송·미실행이 확정된** 실패 | `FAILED` → `IDLE` |
| `UNVERIFIED` | `ACTION`의 실행 여부를 확정할 수 없고 화해 진행 중 | `UNKNOWN_OUTCOME` |
| `UNRESOLVED` | 끝내 확정 불가, 또는 화해할 수 없는 명령의 결과 불명 | `IDLE` |

- `UNVERIFIED`는 **`ACTION`에만** 존재한다. `CHAT`·`BEHAVIOUR`는 결과가 불명이면
  `UNVERIFIED`를 거치지 않고 곧바로 `UNRESOLVED`가 된다. 앱 재시작 뒤 미완료
  컨텍스트도 같다.
- `RECONCILING`은 정상 완료 뒤 확인 단계이며 성공을 뒤집지 않는다. 화면에서
  `UNVERIFIED`("실행 여부를 확인하는 중")와 구분한다.
- **`UNVERIFIED` 종결 상한 `[후보]`**: 재연결 뒤 화해 조회 3회(2·5·10초 간격)에서도
  확정하지 못하거나, 재연결 없이 10분이 지나면 `UNRESOLVED`로 종결한다. 근거는 설계
  판단이며 02 §10의 재연결 백오프와 맞춘 값이다. 실측 뒤 확정한다.
- `UNRESOLVED`의 사용자 표시: `CHAT`은 "전송됐는지 확인할 수 없어요. 다시 보내지
  않았어요", `ACTION`은 "결과를 확인할 수 없어요. 게임에서 직접 확인해주세요". 재실행은
  사용자의 새 요청으로만 가능하다. 재요청이 이중 소모될 수 있는 비용 작업에는 그
  위험을 함께 알린다.
- 화면 결과 용어 대응: `COMPLETED`=완료, `PARTIAL`=일부 완료, `FAILED`=실패,
  `UNVERIFIED`=결과 확인 중, `UNRESOLVED`=확인 불가. 04가 이 5개를 그대로 쓴다.

## 5. 승인 모델

### 5.1 승인이 필요한 경우

`CHAT`(`@@` 직접 입력 예외 제외), `BEHAVIOUR`(단독 실행 시), `ACTION`은 사용자의
명시적 승인이 필요하다. `requiresConfirm`이 누락되거나 알 수 없는 값이면 확인
필요로 처리한다(02 §9).

### 5.2 AI 경로의 승인 카드

AI가 제안한 실행은 선택지 클릭만으로 실행되지 않는다. 클릭하면 승인 카드가 열리고
내용은 AI 문장이 아니라 검증된 `ActionContext`로만 만든다.

| 카드 항목 | 출처 |
|---|---|
| 하는 일 | 중개기가 생성한 `label` |
| 보낼 채널·내용 | 검증된 `args` |
| 비용 | 총비용 합계. 알 수 없으면 "비용 확인 불가" |
| 다회 호출 | 총 호출 횟수와 총비용. 승인 한 번이 컨텍스트 하나 전체에 유효 |
| 게임 정보 신선도 | 카드가 근거로 삼은 상태의 관찰 시각, 오래됐으면 "오래된 정보" |
| 선택지 | `실행`, `취소` — 기본 초점은 `취소` |

- 카드의 `실행`이 승인이다. 승인은 해당 `action_id` 하나에만 유효하다.
- **유효 시간 `[후보: 카드 표시부터 실행 시작까지 2분]`**. 단조 시계를 쓰며 시스템
  시간 변경의 영향을 받지 않는다. 만료되면 카드를 닫지 않고 "시간이 지났어요.
  다시 확인해주세요"로 바꿔 한 번의 조작으로 다시 열게 한다(시간 압박 완화).
- **실행 직전 재검증**: 연결 상태, 재화, 인자가 승인 때와 같은지 확인하고 달라졌으면
  카드를 다시 보여준다. 대기열에서 오래 기다린 승인은 실행하지 않는다.
- 카드가 열려 있는 동안 게임이 끊기면 `실행`을 비활성화하고 이유를 보여준다.
- AI 문장이 카드 내용과 다르면 카드가 우선한다.
- 카드는 입력 초점을 빼앗지 않는다. 사용자가 선택지를 눌렀을 때만 열린다.

### 5.3 `@@` 예외의 범위

`@@` 예외는 **사용자가 입력창에서 직접 확정한 문구**에만 적용한다.

- 예외에 포함되는 것: 사용자가 입력한 문구, 6.1의 정규화, 자동 이모지 1개.
- **자동 행동**은 미리보기(6.3)에 함께 표시된 내용 전체를 사용자가 `보내기`나
  Enter로 확정하는 것을 승인으로 본다. 미리보기 없이 행동이 나가는 경로는 없다.
  설정에서 자동 행동을 끌 수 있다.
- 예외에서 제외되는 것: AI 응답, 선택지 텍스트, 자동 채움, 붙여넣기(5.4).

### 5.4 출처 `origin` 강제

`origin`은 전송 요청에 **UI 입력 처리부가 직접 부여**하며 문자열 내용으로 정해지지
않는다.

| `origin` | 부여 조건 | 승인 |
|---|---|---|
| `USER_AT` | 사용자의 키 입력으로 입력창에서 확정한 한 줄 `@@` 문구 | 5.3 예외 |
| `AI_APPROVED` | 5.2 카드에서 `실행` | 카드가 승인 |
| `SYSTEM` | **`QUERY` 전용.** `CHAT`·`BEHAVIOUR`·`ACTION`에는 사용할 수 없음 | — |

- 입력창을 AI 응답·선택지가 채우면 `AI_PREFILL`로 표시하고 `USER_AT`가 되지 못한다.
  그 문구를 보내려면 5.2의 카드를 거친다.
- **붙여넣기**로 들어온 문구는 `USER_AT`가 아니다. 붙여넣기 문구는 미리보기와 별도
  확인(`이 내용을 보낼까요?`)을 거쳐야 하며 그때 `USER_AT`가 된다.
- 한 번에 **한 줄만** 전송한다. 여러 줄 붙여넣기는 거부하고 이유를 알린다.
- 전송 중에는 `보내기`와 Enter를 잠가 반복 입력을 막는다. 같은 문구를 60초 안에
  다시 보내려 하면 "방금 보낸 말과 같아요. 다시 보낼까요?"로 확인한다 `[후보]`.
- `send_chat`의 인자에는 `origin`이 필수이며 없으면 전송하지 않는다.

## 6. `@@` 채팅 파이프라인

### 6.1 입력 판정과 정규화

화면 모드 표시와 실제 전송은 **같은 함수**를 사용한다(예외를 던지지 않고 결과 값을
반환하는 판정 함수와, 그 값으로 전송하는 함수로 나눈다).

1. 유니코드 NFKC 정규화 뒤 제어문자와 비가시문자(제로폭 공백 등 범주 Cc·Cf)를 제거한다.
   `[가정: 게임이 같은 문자를 무시하거나 거부함이 미검증]`
2. 앞 공백을 제거하고 `@@`로 시작하면 게임 채팅이다. NFKC로 전각 `＠＠`가 `@@`로
   바뀌면 게임 채팅으로 본다. 정규화로 문구가 달라졌으면 미리보기에서 **정규화된 값**을
   보여준다.
3. `@@` 뒤 공백 여부는 같게 취급한다. 본문의 앞뒤 공백을 제거하고 비면 전송하지 않는다.
4. 본문이 `/`나 `#`로 시작하면 거부한다(NFKC로 전각 `／`·`＃`도 걸린다).
5. 개행이 있으면 거부한다(5.4의 한 줄 규칙).

### 6.2 길이

게임 채팅 한도는 50자다. 자동 이모지 접미(공백 1 + 이모지 1 = 2자)를 제외해 사용자
입력은 최대 **48자**다. 계산 단위는 코드포인트이며 게임의 실제 계산 단위는
미검증 `[가정]`이다. 게임이 거부하면 그 결과를 알리고 `FAILED`로 처리한다.

### 6.3 전송 전 미리보기

입력창 아래에 최종 전송 내용을 한 줄로 보여준다: `보낼 말: 고마워요 😍 (행동: /하트)`.
자동 행동이 없으면 그 부분을 생략한다. AI 경로의 채팅에는 자동 이모지·행동을
붙이지 않는다(사용자가 카드에서 본 문구가 곧 전송되는 문구다).

### 6.4 단계 전송과 부분 성공

| 채팅 | 행동 | 결과 | 사용자 표시 | 다음 행동 |
|---|---|---|---|---|
| 성공 | 성공 | `COMPLETED` | 보냈어요 | — |
| 성공 | 실패 | `PARTIAL` | 채팅은 보냈고 행동만 실패했어요. 채팅은 다시 보내지 않아요 | `행동만 다시 보내기` 버튼(채팅 재전송 없음) |
| 미전송 확정 | 미실행 | `FAILED` | 채팅을 보내지 못했어요 | 사용자가 다시 보냄 |
| 결과 불명 | 미실행 | `UNRESOLVED` | 전송됐는지 확인할 수 없어요 | 자동 재전송 금지 |

행동 재전송 버튼은 `@@`가 `/`로 시작하는 입력을 거부하는 규칙과 별개의 전용 경로이며,
허용 행동 목록의 행동만 보낸다.

### 6.5 대기 정책

`ACTION` 실행 중, 게임 연결 문제 중(`SLOW_RESPONSE` 포함), 결과 확인 중에 입력한
`@@`는 **초안 1개**로 보존한다. 초안과 대기 슬롯은 같은 것이다.

- 초안은 **자동으로 발송하지 않는다.** 조건이 해소되면 "지금 보낼까요?"로 알리고
  사용자가 `보내기`를 눌러야 나간다. 화면에 입력한 시각을 함께 보여준다.
- 보낼 때 **초안이 오래됐으면**(입력 뒤 10분 `[후보]`) 문구를 한 번 더 보여주고
  확인받는다. 위치·채널이 바뀌었을 수 있음을 안내한다.
- 창이 숨겨져 있으면 운영체제 알림으로 "보낼 수 있어요"를 알린다. 알림은 초점을
  빼앗지 않는다.
- 새 `@@`를 입력하면 기존 초안을 교체할지 묻는다.
- 게임이 끊긴 동안에도 입력은 막지 않고 초안으로 보존하며 `보내기`만 비활성화하고
  이유를 보인다. 초안은 앱이 실행 중일 때만 보관하고 종료하면 폐기한다(02 §12의
  "보존"은 실행 중 범위다).
- 02 `QUEUED` 상태는 초안이 아니라 **승인된 `ACTION`·AI 조회가 실행 순서를 기다리는
  것**을 뜻한다. 대기 건수는 사용자 명령 1건이며 초안은 여기에 포함되지 않는다.

### 6.6 `write_chat` 응답 판정표

응답은 (종료 코드, 응답 본문 `status`)로 세 가지 중 하나로 판정한다.
**표에 없는 값은 모두 `불명`으로 처리한다.** 성공으로 추측하지 않는다.

| 종류 | 조건 | 결과 |
|---|---|---|
| 전송 확정 | 종료 코드 0이고 `status`가 성공 목록 안 `[가정: 실측 전 후보 목록]` | 성공 |
| 미전송 확정 | `status`가 `rejected`·`invalid_body`, 또는 코드 5와 `game_off`·`option_off` 확인 | `FAILED` |
| 불명 | 위 두 경우가 아닌 모든 것: 시간 초과, 프로세스 종료·EOF, 알 수 없는 코드·`status`, JSON 오류, 사용자 중단(Ctrl+C) | `UNRESOLVED` |

- `blocked`, `timeout`, `canceled`, `retryAfterSeconds`가 있는 응답은 성공이 아니다.
  `retryAfterSeconds`는 사용자에게 보여주되 자동 재시도는 하지 않는다.
- 채팅 전송 프로세스에도 제한 시간 `[후보: 15초]`을 둔다. 초과하면 프로세스를 정리하고
  `UNRESOLVED`로 종결한다.
- 성공 목록과 종료 코드의 의미는 실측으로 확정한다(§12).

## 7. AI 대화 요청 상태

`AIConnectionState`(02 §4.2)는 연결 자체를, 아래는 각 대화 요청의 상태를 뜻한다.
02의 `CommandState`와 별개다.

| 상태 | 의미 | 사용자 행동 |
|---|---|---|
| `AI_IDLE` | 진행 중 요청 없음 | 입력 |
| `AI_THINKING` | 응답 대기 | `중단` |
| `AI_STREAMING` | 응답 수신 중 | `중단` |
| `AI_TOOL_WAITING` | 게임 조회 결과 대기 | `중단` |
| `AI_FAILED` | 실패 확정 | `다시 묻기` |
| `AI_ABORTED` | 사용자가 중단 | 입력 |

- 첫 응답 제한 시간 `[후보: 30초]`, 수신 중 무진행 `[후보: 15초]`. 초과하면
  `AI_FAILED`로 종결하고 사용자에게 알린다.
- 요청이 실패하면 대화에 실패 응답을 남긴다("답을 받지 못했어요. 다시 물어볼까요?").
  질문이 무응답으로 남지 않게 한다.
- 게임 조회가 `SLOW_RESPONSE`나 실패이면 그 사실을 AI에 전달하고, AI가 없으면 로컬
  문구로 답한다.
- `AUTH_ERROR`와 `NOT_CONFIGURED`는 자동 재시도하지 않고 설정을 안내한다.
  `RATE_LIMITED`는 다시 쓸 수 있는 시각을 표시한다.
- AI 응답에서 명령을 추출하면 2장의 검증과 5장의 승인을 거친다. AI 경로의 채팅·행동
  페이로드에도 6.1의 `/`·`#`·개행 검증과 허용 행동 목록 검증을 적용한다.

### 7.1 AI 없이 사용하는 범위

`NOT_CONFIGURED` 또는 `AI 없이 사용` 선택 시 제공하는 기능:

| 기능 | AI 없이 동작 방식 |
|---|---|
| 게임·작업 상태 안내와 복구 | 그대로 |
| `@@` 게임 채팅 | 그대로 |
| 첫 선택지 `오늘 할 일 추천` | 스냅샷 기반 **규칙 문구**(가방 비율·만복도·미션 남은 개수 등을 고정 문장으로 안내) |
| 첫 선택지 `내 상태 쉽게 설명` | 스냅샷을 정해진 문장으로 풀어 표시 |
| `@@ 게임 채팅 사용법` | 준비된 도움말 |
| 자유 질문 | 사용 불가, `AI 연결하기`로 안내 |

### 7.2 AI 설정 진입

설정의 `AI 연결`에서 `연결하기`를 누르면 (1) 공급자 선택(첫 버전은 기본 공급자
하나), (2) 자격증명 입력, (3) 연결 확인의 3단계를 밟는다. 게임 연결 검사(02 §12)와
독립적이며 언제든 건너뛰거나 나중에 할 수 있다. 공급자 종류와 자격증명 보관 방식은
별도 설계 후 승인한다. 자격증명 전체 값은 화면·로그·진단 정보에 표시하지 않는다.

## 8. 저장과 개인정보

- 사용자 채팅 문구와 AI 대화 내용은 로그·진단 정보·`ActionContext` 보존 파일에
  기록하지 않는다. 필드별 저장 허용은 3장 표가 정한다.
- 대화 기록의 기본값은 현재 실행 중에만 보관하고 종료 시 폐기한다. 저장은 사용자가
  켰을 때만 하며 저장 범위와 삭제 방법을 설정에서 보여준다. 알림 숨김·제안 끄기
  설정은 별도로 재시작 뒤에도 유지하며 개인정보를 포함하지 않는다.
- CLI 실패 시 오류 표시에 CLI stdout·stderr 원문을 넣지 않는다(채팅 문구가 에코될 수
  있음). 사용자에게는 분류된 메시지만 보이고 원문은 진단 모드에서 가려서만 다룬다.
- **채팅 문구는 CLI 인자(Base64)로 전달된다.** Base64는 암호화가 아니므로 같은 PC의
  다른 프로세스나 프로세스 생성 감사 로그에서 보일 수 있다. 이 한계를 진단 화면에
  명시한다.
- **진단 정보 복사**는 허용 목록 방식이다. 앱 버전, CLI 버전, 상태 값, 종료 코드, 시각만
  포함한다. Windows 사용자명이 든 경로는 `%USERPROFILE%`로 치환하고, 응답 본문(직업·서버·
  주변 플레이어 이름 등)은 포함하지 않는다. 복사 전에 내용을 미리 보여준다.
- 진단 화면에 "이 앱 밖에서 실행하는 CLI 호출은 막을 수 없어요"를 명시한다.
- 실제 응답 표본을 테스트 자료로 저장할 때는 개인정보를 제거하고 Gitea 저장소에
  올리기 전에 검사한다.
- **외부 AI 공급자로 나가는 정보**: 게임 조회 결과 중 요청과 관련된 필드만 보낸다.
  주변 플레이어 이름 같은 제3자 정보는 기본 제외한다. 게임에서 온 텍스트(이름·설명)는
  AI에 "신뢰할 수 없는 데이터"로 표시해 전달하고, 그 안의 지시는 따르지 않는다.
  AI 설정 때 전송되는 정보 종류를 한 번 알린다.

## 9. 테스트와 합격 기준

02 §15의 형식(사전 상태 / 입력·사건 / 기대 내부 상태 / 사용자 표시 / 사용 가능
기능 / 재시도 규칙)을 따른다. 아래 표는 이 문서의 필수 시나리오다.

| # | 사전 상태 | 입력·사건 | 기대 내부 상태 | 사용자 표시 | 재시도 규칙 |
|---|---|---|---|---|---|
| T1 | 준비 | `@@ 안녕` | `RUNNING_SEND`→`IDLE`, `COMPLETED` | 보냈어요 | — |
| T2 | 준비 | 앞 공백 ` @@안녕` | 게임 채팅으로 판정, 화면 모드와 일치 | 게임 채팅 모드 | — |
| T3 | 준비 | `＠＠안녕` | NFKC로 게임 채팅 판정 | 정규화 값 표시 | — |
| T4 | 준비 | 49자, 48자 경계 | 49자 거부, 48자 허용 | 글자 수와 이유 | — |
| T5 | 준비 | `@@ /지역`, `@@ #x`, 제로폭 뒤 `/` | 거부 | 이유 안내 | — |
| T6 | 준비 | 여러 줄 붙여넣기 | 거부 | 한 줄만 보낼 수 있어요 | — |
| T7 | 준비 | 채팅 성공·행동 실패 | `PARTIAL` | 채팅만 보냄 | 행동만 재전송 가능 |
| T8 | 준비 | 채팅 무응답(15초) | `UNRESOLVED` | 확인 불가, 다시 보내지 않음 | 자동 재전송 금지 |
| T9 | 준비 | 응답 `status`가 성공 목록 밖 | `UNRESOLVED` | 확인 불가 | 자동 재전송 금지 |
| T10 | 준비 | 응답이 JSON 배열 | `PROTOCOL_ERROR`, 전송 결과 `UNRESOLVED` | 확인 불가 | 자동 재전송 금지 |
| T11 | `RUNNING_ACTION` | `@@` 입력 | 초안 보존 | 대기 이유와 초안 | 조건 해소 뒤 `보내기` 필요 |
| T12 | 초안 보존 | 재연결·작업 종료 | 자동 발송 없음 | "지금 보낼까요?" | 사용자 조작만 |
| T13 | 초안 10분 경과 | `보내기` | 문구 재확인 | 오래된 문구 안내 | — |
| T14 | AI가 채팅 제안 | 선택지 클릭 | 카드 열림, 실행 아님 | 검증된 내용·비용 | 카드 `실행`만 승인 |
| T15 | 카드 열림 | 2분 경과 | 카드 만료 재열기 | 시간이 지났어요 | 한 번 조작으로 재열기 |
| T16 | 입력창 AI 채움 | 전송 | `AI_PREFILL` → 카드 필요 | 카드 | `USER_AT` 예외 미적용 |
| T17 | 붙여넣기 | 전송 | 확인 뒤에만 `USER_AT` | "보낼까요?" | — |
| T18 | 준비 | 알 수 없는 명령·`requiresConfirm` | 거부 또는 승인 필요 | 안내 | — |
| T19 | 전송 중 | Enter 반복 | 잠금, 중복 없음 | 전송 중 | — |
| T20 | 동일 문구 60초 이내 | 재전송 시도 | 확인 필요 | 같은 말이에요 | 사용자 확인 |
| T21 | `ACTION` 실행 중 | 앱 강제 종료 후 재시작 | `UNVERIFIED` 진입 | 결과 확인 중 | 자동 재실행 금지 |
| T22 | `CHAT` 전송 중 | 앱 강제 종료 후 재시작 | `UNRESOLVED` | 확인 불가 | 자동 재전송 금지 |
| T23 | `UNVERIFIED` | 상한(3회 또는 10분) | `UNRESOLVED` | 게임에서 직접 확인 | 새 요청만 |
| T24 | AI 요청 | 무응답 30초 | `AI_FAILED`, 대화 실패 응답 | 답을 받지 못했어요 | 사용자 `다시 묻기` |
| T25 | AI 요청 | 게임 조회 실패 | 실패 사실을 응답에 반영 | 조회 실패 안내 | — |
| T26 | 미설정 | 첫 선택지 | 규칙 문구 | 준비된 문구 | — |
| T27 | 진단 | 복사 | 허용 목록만, 미리보기 | 복사 내용 | 로그·진단에 채팅 문구가 없음을 자동 검사 |

실제 게임 상태를 바꾸는 검증은 별도 사용자 승인 뒤에 수행한다. `write_chat`의 실제
응답 표본(성공 `status`, 종료 코드)은 채팅 하나를 사용자 승인 아래 보내는 검증으로
확보한다.

### 9.1 합격 기준

- 자동 테스트 T1~T27 전부 통과, 모의 CLI 기반
- 예상하지 못한 응답에서 자동 상태 변경 명령 0건, 중복 채팅 전송 0건
- 로그·진단 정보에 채팅 문구가 없음을 자동 검사로 확인
- **사용성 시험**: 대상은 AI에 익숙하지 않은 마비노기 모바일 이용자 5명 `[후보]`.
  과업 4개 — (1) 상태 3개를 각각 설명, (2) `@@` 채팅 전송, (3) 게임 꺼짐 상태 복구,
  (4) 승인 카드에서 실행 또는 취소. **각 과업에서 5명 중 4명 이상 도움 없이 성공**해야
  통과한다.
- **접근성 수치**: 본문 대비 4.5:1 이상, 아이콘·경계 등 UI 요소 3:1 이상, 터치 대상 최소
  44×44px, Windows 고대비 테마 2종 이상(어두움·밝음), 배율 100·125·150·200%에서 핵심
  행동이 잘리지 않음 `[후보: WCAG 2.2 기준을 준용]`.
- 높은 중요도 서브에이전트 리뷰 문제 0건

## 10. 구현 시 반영할 기존 코드 문제 (구현 2단계)

현재 `mabinogi_chat.py`의 문제:

- 오류 없음을 성공으로 판정한다(6.6이 요구하는 판정표와 다름).
- 0이 아닌 모든 종료 코드를 "게임에 연결하지 못했습니다"로 단정한다.
- 전송 프로세스에 제한 시간이 없다. 전송 중 Ctrl+C가 안내 없이 종료된다.
- 응답이 JSON 배열이면 처리되지 않은 예외로 프로세스가 종료된다.
- 채팅 성공 뒤 행동 실패를 하나의 예외로 합쳐 "채팅은 나갔다"는 정보가 사라진다.
- 알 수 없는 `status`를 성공으로 처리한다(02 §6.2와 어긋남).
- 입력 판정이 예외 기반이라 모드 표시용 판정과 분리되어 있다. 정규화(NFKC·비가시문자)가 없다.
- 부분 문자열 규칙으로 행동을 붙인다(`사과 팔아요`에 사과 행동, `안 고마워`에 하트).
  부정·문맥 예외가 필요하며 README의 "뜻이 분명하면" 표현도 맞춰 조정한다.
- 오류 출력에 CLI stdout 전체를 넣는다.

## 11. 02와의 관계

02는 게임·AI·명령 세 축의 상태와 전이를, 이 문서는 명령 축 내부의 의미와 안전 규칙을
다룬다. 02 §4.3, §5, §7, §16이 이 문서를 참조한다. 두 문서의 용어 대응은 02 §4.4를
따른다.

## 12. 남은 검증 항목

02 §18에 없는 것만 적는다.

1. `write_chat`의 성공·거부·불명 응답과 종료 코드 (6.6의 성공 목록)
2. `capabilities`에 명령 분류·비용 메타데이터가 있는지 (2장, 3장)
3. 허용 행동 목록을 조회하는 게임 명령과 응답 (2장)
4. 게임의 채팅 길이 계산 단위, 비가시문자·전각 문자 처리 (6.1, 6.2)
5. 게임이 동일 채팅을 중복 제거하는지 (5.4의 60초 규칙)
6. 코드 5 응답 본문으로 `game_off`·`option_off`를 구분할 수 있는지 (02 §5)

## 13. 리뷰 기록

| 날짜 | 상태 | 내용 |
|---|---|---|
| 2026-09-21 | 검토 대기 | 02 리뷰와 04 UI 리뷰에서 확인된 기능 설계 공백을 채워 작성 |
| 2026-09-21 | 변경 필요 | 서브에이전트 3인 리뷰, 승인 보류 (`reviews/02-03-기능설계-리뷰-2.md`) |
| 2026-09-21 | 재검토 대기 | 응답 판정표, `origin` 강제, 명령 상태 경로, 결과 종결 상한, 저장 허용 목록, 테스트 표, AI 없는 범위 반영 |
