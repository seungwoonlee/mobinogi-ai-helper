# UI 상세설계 — 서브에이전트 독립 리뷰

| 항목 | 내용 |
|---|---|
| 대상 문서 | `05-UI-상세설계.md` |
| 리뷰 일자 | 2026-09-21 |
| 리뷰 방법 | 독립 서브에이전트 2건 (UX·접근성 / 아키텍처·안전) |
| 종합 판정 | 승인 보류 — 변경 필요 |
| 높은 중요도 | UX 8건, 아키텍처 6건 (중복 통합 후 아래 8개 묶음) |

## 1. 높은 중요도 발견 사항

| # | 발견 | 근거 | 필요한 조치 |
|---|---|---|---|
| H1 | 02의 상태(게임 12·AI 6·명령 8) 중 05가 다루는 것은 일부뿐. 상태→화면 매핑표 부재 | 05 §4.3·§4.4·§6, 02 §4 | 상태별 표시 문구·주 행동·입력 가능 범위·라이브 영역 수준 표 추가 |
| H2 | 조합 표가 5행뿐. 게임 끊김 중 작업, 재시작 후 결과 불명, AI 오류×게임 조합 누락 | 05 §6 | 조합 표 확장, "입력 가능 범위" 열 추가 |
| H3 | `RECONCILING`/`UNKNOWN_OUTCOME`/`일부 완료` 표현이 02 모델과 불일치. 채팅은 상태 비교로 화해 불가 | 05 §3.2·§4.6, 02 §5·§9·§11 | 02에 결과 분류와 전이 추가, 채팅의 결과 불명은 재전송 금지·확인 불가로 종결 |
| H4 | 대기 중인 `@@`의 발송 시점이 모호. 자동 발송 시 맥락이 지난 공개 채팅이 나감 | 05 §2·§4.5, 02 §7 | 재연결·작업 종료 뒤에도 사용자의 명시적 `보내기` 필요로 확정 |
| H5 | AI 경로의 확인 UI 부재. 선택지 문구(AI 작성)와 실제 명령이 달라질 위험. `@@` 예외가 자동 행동까지 포함하는지 불명확 | 05 §2·§3.2·§8, README §4.1, 02 §9 | 승인 화면은 중개기의 구조화 명령·비용 메타데이터로 생성, `@@` 예외는 사용자가 직접 친 입력에만 적용, 자동 이모지·행동 사전 표시 |
| H6 | 첫 실행에 AI 설정 경로 없음. 미설정 시 첫 브리핑 대체 화면 없음 | 05 §4.1·§4.2·§5 | 첫 실행 흐름에 AI 설정·`AI 없이 사용` 포함 |
| H7 | AI 무응답·장시간 응답·스트리밍 끊김 처리 없음. 연결 오류 시 사용자 질문에 답이 남지 않을 수 있음 | 05 §3.2·§4.4 | AI 요청 상태·중단·시간 제한, 대화 내 실패 응답 정의 |
| H8 | 승인 기준이 측정 불가. 화면이 요구하는 값(작업 이름·시작 시각 등)이 02 모델에 없음. 05가 승인 전인 02에 의존하는데 구현 순서·관문이 없음 | 05 §10, 02 §15·§16 | 정량 기준(참가자·통과 비율·대비율·배율 목록), 02에 실행 컨텍스트 추가, 05는 02 승인 전까지 목업 전용으로 명시 |

## 2. 중간 중요도 요약

- 접근성: 포커스·탭 순서·`Enter` 동작, UIA 매핑(웹 용어 `aria-live` 대체), 터치 대상 크기, 라이브 영역 과다 알림 디바운스.
- `@@` 입력: 입력 한도는 48자(50자에서 이모지 접미 2자 제외), `/`·`#` 시작 거부, 앞 공백이 있을 때 화면 모드와 실제 전송 판정 불일치. 판정 함수를 하나로 통일한다.
- 게임 오류 문구가 02보다 단정적이다(`GAME_UNAVAILABLE`은 원인 미확정 포함). 05 §8의 "확인 안 된 것은 단정하지 않는다"와 충돌.
- 앱 닫기·숨김: 트레이·재열기·완료 알림·강제 종료 뒤 복구 화면 부재.
- 자동으로 하는 것은 상태 확인뿐이며 채팅·작업은 자동 실행되지 않는다는 공통 문구 부재.
- 신선도 표시(`fresh/stale/partial/invalid`)와 카드 우선순위(긴급/도움/참고), 개인정보(대화 저장 범위, 진단 복사 미리보기) 규정 부족.
- 장시간 작업 중 짧은 조회: 캐시 값 사용인지 대기인지 미정.

## 3. 낮은 중요도 요약

용어 대응표(05 화면 용어 ↔ 02 상태명), 게임 내 설정명 `MM AI 에이전트` 표기, 내부 용어 "화해 진행" 노출, `@@안녕하세요` 예시의 행동 누락, 목업 시나리오 6개로 부족, 05에 리뷰 기록 절 없음, `AIProvider` 언급이 02 제외 범위와 걸림.

## 4. 코드 관련 발견 (`mabinogi_chat.py`)

- 채팅 전송 뒤 행동 전송이 실패하면 예외 하나로 뭉쳐져 "채팅은 이미 나갔다"는 정보가 사라진다. 사용자가 다시 보내면 채팅이 중복된다.
- 응답이 JSON 배열이면 `response.get`에서 일반 예외가 발생해 `PROTOCOL_ERROR`로 분류되지 않는다.

## 5. 승인을 위한 최소 조치

1. 02에 실행 컨텍스트(`action_id`, `label`, `started_at`)와 결과 분류, `UNKNOWN_OUTCOME` 종결 전이를 추가하고 재검토한다.
2. 05에 상태→화면 매핑표와 확장된 조합 표를 추가한다.
3. 대기 `@@`, AI 경로 확인 UI, `@@` 예외 범위(자동 이모지·행동 포함 여부), 부분 성공을 확정한다.
4. 첫 실행 AI 설정 경로와 AI 무응답 처리를 정의한다.
5. 승인 기준(§10)을 정량화하고 05를 목업 전용 문서로 명시한다.
6. 수정본을 서브에이전트가 다시 리뷰한 뒤 사용자 승인을 기록한다.

높은 중요도 문제가 0건이 되기 전에는 UI 구현 단계로 넘어가지 않는다.
