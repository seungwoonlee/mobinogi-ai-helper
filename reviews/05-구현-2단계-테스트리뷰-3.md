# 구현 2단계(CLI 기반) — 테스트 서브에이전트 독립 리뷰 3회차

| 항목 | 내용 |
|---|---|
| 리뷰일 | 2026-09-21 |
| 대상 커밋 | `f710760771566bd79daa4588ab65ef22595da966`(브랜치 `feat/stage2-cli-base-wjt6e4`) |
| 대상 | `tests/` 전체(144건, 1 skip), 관련 프로덕션 코드(`mobinogi_helper/response_validator.py`, `mobinogi_helper/response_judge.py`) |
| 리뷰어 | 테스트(독립 재검증, 2회차 문서 이후 코드만 근거로 재확인) |
| 결론 | **승인**(높음 0건) |

## 1. 2회차 지적 해소 확인

2회차 테스트 리뷰(`reviews/05-구현-2단계-테스트리뷰-2.md`, 높음 1·중간 4·낮음 3)의 각 항목을 커밋 메시지가 아니라 코드 자체로 재추적했다. `f710760`이 `7bdab26` 대비 실제로 바꾼 파일은 `mobinogi_helper/response_validator.py`, `tests/test_broker_chat.py`, `tests/test_leaks.py`, `tests/test_response_judge.py`, `tests/test_review_fixes_2.py`(신규) 다섯 개뿐이다(`git diff 7bdab26 f710760 --stat`로 확인).

| # | 지적 | 판정 | 근거 |
|---|---|---|---|
| H1 | `retryAfterSeconds: 0`이 `0 == False` 때문에 "마커 없음"으로 오판정되어 `SENT`로 확정됨 | **해소됨** | `mobinogi_helper/response_validator.py:96-106`의 `find_marker`가 `if value is None or value == "" or value is False: continue`로 바뀌었다. `False`는 `is False`(식별 비교)로만 배제되므로 `0`은 어떤 분기에도 걸리지 않고 그대로 `return value`된다. 반면 `None`·`""`·`False`는 여전히 올바르게 배제된다. `response_judge.py`에서 이 함수가 쓰이는 마커 키는 `"error"`와 `"retryAfterSeconds"` 두 개뿐(`response_judge.py:110`, `:119`)인데, `"error": false`가 "오류 없음"을 뜻하는 스키마이므로 `False`를 계속 배제하는 것은 의미상 올바르다. **실증**: 수정을 임시로 되돌려(`value not in (None, "", False)`) 3개 회귀 테스트를 돌리면 모두 실패한다 — `test_retry_after_zero_is_still_a_marker`(`None != 0.0`), `test_markers_block_success`의 `retryAfterSeconds=0` 서브케이스(`SENT != UNKNOWN`), `test_review_fixes_2.py::RetryAfterZeroTests::test_retry_after_zero_does_not_confirm_send`(`COMPLETED != UNRESOLVED`). 확인 후 원본 코드로 정확히 복원했다(`git status`/`git diff` clean 재확인). |
| M1 | `test_shipped_config_is_empty_and_unverified`가 저장소의 실제 `config/known_responses.json`을 직접 읽어 향후 표본 확보 시 깨질 위험 | **해소됨** | `tests/test_response_judge.py:160-166`이 이제 `self._load(...)`(임시 파일)에 `json.dumps({"schema": 1, "config_verified": False, "sent_statuses": [], "rejected_statuses": []})` 고정 픽스처를 써서 검사한다. 이 내용은 실제 `config/known_responses.json`(확인함, 4줄 모두 동일)과 현재는 같지만, 이제는 실제 파일이 바뀌어도(표본이 채워져도) 이 테스트는 그 파일을 읽지 않으므로 깨지지 않는다. |
| M2 | 가드가 `asyncio` 서브프로세스 생성 함수를 감싸지 않음 | **문서화만 함(타당)** | `tests/__init__.py`를 다시 확인해도 `asyncio` 관련 패치는 없다(그대로임, 코드 수정 없음). 대신 `reviews/05-구현-2단계-결과.md:54-55`, `:80-81`(§3 "알려진 제한")에 "테스트 가드는 `Popen`·`os.system`·`os.spawn*`·`_winapi.CreateProcess`를 감싸지만 `asyncio` 서브프로세스 생성 함수는 감싸지 않는다(현재 프로덕션 코드는 이를 쓰지 않는다)"고 명시하고 7단계 착수 전 재검토 과제로 남겼다. `mobinogi_helper/*.py`·`mabinogi_chat.py` 전체에 `asyncio` 참조가 없음을 재확인했다(`grep -rn asyncio` 매치 없음). 현재 프로덕션 코드가 asyncio를 전혀 쓰지 않는 한 이 결정은 타당하다. |
| M3 | 표준 출력 유출 검사에서 6자 부분 조각(`CANARY[:6]`)이 위치 제약 없이 면제됨 | **해소됨** | `tests/test_leaks.py:101-104`가 `if CANARY in line`에서 `if any(needle in line for needle in (CANARY, CANARY[:6], SENT_TEXT))`로 바뀌었다. 이제 `CANARY`·`CANARY[:6]`·`SENT_TEXT` 중 하나라도 포함된 줄은 반드시 `"보낼 말:"`로 시작해야 한다. `SENT_TEXT[:6]`은 별도로 나열되지 않았지만, `CANARY`가 6자보다 길고 `SENT_TEXT = CANARY + " 😊"`이므로 `SENT_TEXT[:6] == CANARY[:6]`이 문자열로 항상 같다(직접 실행해 확인: `'카나리문구입' == '카나리문구입'` → `True`). 따라서 목록에서 빠진 것이 아니라 애초에 같은 문자열이라 빠질 필요가 없다. Base64 6자 조각(`std[:6]`) 등 원래도 면제 대상이 아니었던 변형은 여전히 전면 검사(위치 무관) 대상이라 회귀 없음. |
| M4 | `_winapi.CreateProcess` 가드가 Linux에서 검증 불가 | **문서화만 함(타당, 변경 없음)** | `tests/test_cli_adapter.py:167-171`의 `test_low_level_create_process_guard`는 여전히 `import _winapi` 실패 시 `skipTest`로 건너뛴다(Linux에서 실제로 skip, `skipped=1`이 이 테스트). 이 환경에서 `_winapi`를 스텁으로 주입해 플랫폼 무관 단위 시험을 추가하는 개선은 이번 라운드에도 반영되지 않았지만, `reviews/05-구현-2단계-결과.md`의 "알려진 제한"에 "Windows에서 별도 실행 검증이 필요하다"고 이미 명시돼 있어 문서화 원칙은 지켜졌다. 1차 방어선(`subprocess.Popen` 몸통 교체)은 이 환경에서 실행으로 검증되므로(`tests/test_cli_adapter.py:186-194`), 승인을 막을 정도의 결함은 아니다. |
| L1 | taskkill 허용 규칙이 파일명만 비교 | **미해소(그대로 남음)** | `tests/__init__.py:50`, 변경 없음(`os.path.basename(first) == "taskkill.exe"`). 테스트 전용 가드의 방어 강도 이슈이며 실 CLI를 겨냥하는 경로가 아니라 이번 단계 승인을 막을 사안이 아니다. |
| L2 | 허용 PID 집합이 정리되지 않고 누적 | **미해소(그대로 남음)** | `tests/__init__.py:89-90`, 변경 없음(`_State.pids.add(self.pid)`, 제거 코드 없음). 마찬가지로 테스트 전용 자산의 위생 문제이며 안전 보증에 구멍을 내지 않는다. |
| L3 | 채팅 경로 퍼징이 항상 빈 설정만 사용해 "정상 성공 인식" 방향은 검증하지 않음 | **미해소(그대로 남음, 승인 저해 아님)** | `tests/test_broker_chat.py:357-379`의 `FuzzTests`는 여전히 `empty_known = KnownResponses()` 고정이다. 이번 라운드는 퍼징 후보에 `retryAfterSeconds: 0`과 `"["*5000`만 추가했을 뿐(§2에서 확인) `KNOWN_FIXTURE` 방향 퍼징은 추가되지 않았다. 계획서의 "0건 오탐" 요구는 여전히 충족하지만, 퍼징의 커버리지가 계획 취지보다 좁다는 점은 그대로다. |

## 2. 새로 발견한 문제

`f710760`이 실제로 건드린 5개 파일(§1 서두)과 `tests/*.py` 전체를 다시 훑었다. `test_review_fixes_2.py`의 `DeeplyNestedResponseTests`·`RetryAfterZeroTests`는 실제로 서비스 계층(`ChatService.send_chat_action`)까지 통과시켜 `Outcome`·`Reason`을 단언하는 유의미한 테스트이며(허수아비 아님), H1은 §1에서 실증한 대로 되돌리면 확실히 실패한다. `test_broker_chat.py`의 퍼징 후보 추가, `test_leaks.py`의 위치 제약 확장도 §1에서 코드로 확인했다. 이 과정에서 아래 낮은 심각도 항목 1건을 새로 발견했다. 높음·중간 신규 결함은 없다.

### 높음

없음.

### 중간

없음.

### 낮음

| # | 발견 | 파일:줄 | 왜 문제인가 | 재현·검증 |
|---|---|---|---|---|
| N1 | M1 수정으로 저장소의 실제 `config/known_responses.json`을 로드해보는 테스트가 스위트에서 완전히 사라졌다(`load_known_responses()`를 인자 없이 호출하는 곳이 `tests/` 전체에 하나도 없다) | `tests/test_response_judge.py:160-166`(픽스처로 대체된 지점), `mobinogi_helper/response_judge.py:49-51`(`load_known_responses(path=None)`이면 `DEFAULT_KNOWN_PATH`를 읽음) | M1 자체는 올바른 수정이지만(계획 §5 항목4가 요구하는 방향), 그 대가로 "실제 출고 파일이 문법적으로 유효한 JSON이고 필수 키를 갖췄는가"를 확인하는 마지막 테스트까지 없어졌다. `load_known_responses`가 fail-closed(파일이 깨져도 빈 설정+경고로 조용히 대체, `response_judge.py:57-65`)라 당장 크래시로 이어지지는 않지만, 누군가 실수로 `config/known_responses.json`을 깨뜨려도(예: 쉼표 누락) 테스트 스위트는 통과하고 사용자는 실행 시점에야 "확인 불가" 경고로 알아차리게 된다 | `grep -rn "load_known_responses()" tests/` → 매치 없음(인자 없는 호출이 전무함을 확인). 권고: `DEFAULT_KNOWN_PATH`를 직접 `load_known_responses(DEFAULT_KNOWN_PATH)`로 읽어 `problem is None`이고 스키마 키(`config_verified`·`sent_statuses`·`rejected_statuses`)가 있는지만 확인하는 가벼운 "파일이 유효한 JSON인가" 테스트를 별도로 추가하면 M1의 취지(내용 변경에 안 깨짐)와 이 커버리지를 동시에 만족한다. 승인을 막을 사안은 아니다. |

## 3. 테스트 실행 결과

`python3 -m unittest discover -s tests -t .`를 5회 반복 실행했다(모두 동일 결과).

| 회차 | 결과 | 소요 시간 |
|---|---|---|
| 1 | `Ran 144 tests ... OK (skipped=1)` | 28.968s |
| 2 | `Ran 144 tests ... OK (skipped=1)` | 29.057s |
| 3 | `Ran 144 tests ... OK (skipped=1)` | 29.013s |
| 4 | `Ran 144 tests ... OK (skipped=1)` | 29.128s |
| 5 | `Ran 144 tests ... OK (skipped=1)` | 29.076s |

5회 모두 통과·skip 개수가 동일해 flaky 징후는 없었다(2회차 리뷰가 지적했던 `test_keyboard_interrupt_during_send_is_unresolved_and_skips_behaviour`의 비결정성은 `tests/test_broker_chat.py:340-347`에서 고정 횟수 대신 모의 CLI 호출 로그 관찰로 이미 바뀌어 있었고, 이번 5회 반복에서도 흔들리지 않았다). skip 1건은 여전히 `test_low_level_create_process_guard`(`_winapi` 부재, §1 M4)다.

별도로 H1 회귀 테스트 3건을 `find_marker`의 구 버전(`value not in (None, "", False)`)으로 되돌린 상태에서 실행해 모두 실패함을 실증했고(§1 H1 참고), 확인 직후 `git diff`가 비어 있음을 재확인해 원상 복구를 검증했다.

## 4. 결론

2회차에서 지적된 높음 1건(H1)·중간 4건(M1~M4)·낮음 3건(L1~L3) 중 H1·M1·M3는 코드 추적과 실제 되돌리기 실증으로 완전히 해소됐음을 확인했고, M2·M4는 계획대로 문서화만 하는 것으로 처리됐으며 그 판단은 현재 프로덕션 코드 범위(asyncio 미사용, Windows 전용 계층)를 고려할 때 타당하다. L1~L3는 그대로 남아 있으나 테스트 전용 자산의 위생 문제이거나 퍼징 커버리지의 알려진 한계일 뿐, 안전 보증(실제 CLI 호출 0건, 자동 상태 변경 0건)에 구멍을 내지 않는다.

새로 발견한 결함은 낮음 1건(N1: 실제 출고 설정 파일의 파싱 가능 여부를 확인하는 테스트가 사라짐)뿐이며, `load_known_responses`가 fail-closed로 설계돼 있어 사용자에게 크래시가 아닌 경고로만 나타나므로 이번 단계 승인을 막을 사안이 아니다.

`python -m unittest discover -s tests -t .`를 5회 반복 실행해 모두 `Ran 144 tests ... OK (skipped=1)`로 안정적으로 통과했다.

**높음 0건 → 승인.** N1(낮음)은 다음 리뷰 전 가벼운 보완을 권고한다.
